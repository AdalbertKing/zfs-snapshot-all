# BEGIN zfs-backup-managed (generated by gen-cron.sh -- do not hand-edit, re-run gen-cron.sh instead)
59 * * * * echo "$(date -Is) ZFS-JOB BEGIN arc hourly backup (alpha-data)" >>/LOG; e=$(mktemp 2>/dev/null) || e=/LOG.err.$$; /REPO/snapget.sh -m "automated_" -i -K /root/.ssh/pairing/alpha_ed25519 -A -L alpha "zfsbackup-arc@10.0.0.1:tank/data" "hdd/backups/arc/alpha" 2>"$e"; rc=$?; cat "$e" >>/LOG; echo "$(date -Is) ZFS-JOB END arc hourly backup (alpha-data) rc=$rc" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "arc hourly backup (alpha-data)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"
59 * * * * echo "$(date -Is) ZFS-JOB BEGIN arc hourly backup (beta-data2)" >>/LOG; e=$(mktemp 2>/dev/null) || e=/LOG.err.$$; /REPO/snapget.sh -m "automated_" -i -K /root/.ssh/pairing/beta_ed25519 -A -L beta "zfsbackup-arc@10.0.0.2:tank/data2" "hdd/backups/arc/beta" 2>"$e"; rc=$?; cat "$e" >>/LOG; echo "$(date -Is) ZFS-JOB END arc hourly backup (beta-data2) rc=$rc" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "arc hourly backup (beta-data2)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"
59 * * * * echo "$(date -Is) ZFS-JOB BEGIN arc hourly backup (plain)" >>/LOG; e=$(mktemp 2>/dev/null) || e=/LOG.err.$$; /REPO/snapsend.sh -m "automated_" "tank/plain" 2>"$e"; rc=$?; cat "$e" >>/LOG; echo "$(date -Is) ZFS-JOB END arc hourly backup (plain) rc=$rc" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "arc hourly backup (plain)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"

30 2 * * * echo "$(date -Is) ZFS-JOB BEGIN arc hourly prune (alpha-data)" >>/LOG; e=$(mktemp 2>/dev/null) || e=/LOG.err.$$; /REPO/delsnaps.sh "hdd/backups/arc/alpha/tank/data" "automated_" -H24 2>"$e"; rc=$?; cat "$e" >>/LOG; echo "$(date -Is) ZFS-JOB END arc hourly prune (alpha-data) rc=$rc" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "arc hourly prune (alpha-data)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"
30 2 * * * echo "$(date -Is) ZFS-JOB BEGIN arc hourly prune (beta-data2)" >>/LOG; e=$(mktemp 2>/dev/null) || e=/LOG.err.$$; /REPO/delsnaps.sh "hdd/backups/arc/beta/tank/data2" "automated_" -H24 2>"$e"; rc=$?; cat "$e" >>/LOG; echo "$(date -Is) ZFS-JOB END arc hourly prune (beta-data2) rc=$rc" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "arc hourly prune (beta-data2)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"
30 2 * * * echo "$(date -Is) ZFS-JOB BEGIN arc hourly prune (plain)" >>/LOG; e=$(mktemp 2>/dev/null) || e=/LOG.err.$$; /REPO/delsnaps.sh "tank/plain" "automated_" -H24 2>"$e"; rc=$?; cat "$e" >>/LOG; echo "$(date -Is) ZFS-JOB END arc hourly prune (plain) rc=$rc" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "arc hourly prune (plain)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"
45 3 * * * echo "$(date -Is) ZFS-JOB BEGIN arc keep_daily prune (alpha)" >>/LOG; e=$(mktemp 2>/dev/null) || e=/LOG.err.$$; /REPO/delsnaps.sh -R "hdd/backups/arc/alpha" "automated_daily" -d7 2>"$e"; rc=$?; cat "$e" >>/LOG; echo "$(date -Is) ZFS-JOB END arc keep_daily prune (alpha) rc=$rc" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "arc keep_daily prune (alpha)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"

*/15 * * * * d=$(/REPO/check-snap-age.sh -L alpha "hdd/backups/arc/alpha/tank/data" "automated_" 90m 3h 2>&1); rc=$?; [ -n "$d" ] && echo "$d" >>/LOG; [ $rc -eq 1 ] && /WARN "arc hourly getting stale (alpha-data)" "$d" 2>>/LOG; [ $rc -eq 2 ] && /NOTIFY "arc hourly stale (alpha-data)" "$d" 2>>/LOG; [ $rc -ge 3 ] && /NOTIFY "arc hourly monitor BROKEN (alpha-data)" "$d" 2>>/LOG
*/15 * * * * d=$(/REPO/check-snap-age.sh -L beta "hdd/backups/arc/beta/tank/data2" "automated_" 90m 3h 2>&1); rc=$?; [ -n "$d" ] && echo "$d" >>/LOG; [ $rc -eq 1 ] && /WARN "arc hourly getting stale (beta-data2)" "$d" 2>>/LOG; [ $rc -eq 2 ] && /NOTIFY "arc hourly stale (beta-data2)" "$d" 2>>/LOG; [ $rc -ge 3 ] && /NOTIFY "arc hourly monitor BROKEN (beta-data2)" "$d" 2>>/LOG
*/15 * * * * d=$(/REPO/check-snap-age.sh "tank/plain" "automated_" 90m 3h 2>&1); rc=$?; [ -n "$d" ] && echo "$d" >>/LOG; [ $rc -eq 1 ] && /WARN "arc hourly getting stale (plain)" "$d" 2>>/LOG; [ $rc -eq 2 ] && /NOTIFY "arc hourly stale (plain)" "$d" 2>>/LOG; [ $rc -ge 3 ] && /NOTIFY "arc hourly monitor BROKEN (plain)" "$d" 2>>/LOG
*/15 * * * * d=$(/REPO/check-snap-age.sh -R -L alpha "hdd/backups/arc/alpha" "automated_daily" 30h 48h 2>&1); rc=$?; [ -n "$d" ] && echo "$d" >>/LOG; [ $rc -eq 1 ] && /WARN "arc keep_daily getting stale (alpha)" "$d" 2>>/LOG; [ $rc -eq 2 ] && /NOTIFY "arc keep_daily stale (alpha)" "$d" 2>>/LOG; [ $rc -ge 3 ] && /NOTIFY "arc keep_daily monitor BROKEN (alpha)" "$d" 2>>/LOG
# END zfs-backup-managed
