# BEGIN zfs-backup-managed (generated by gen-cron.sh -- do not hand-edit, re-run gen-cron.sh instead)
59 * * * * e=$(mktemp); /REPO/snapsend.sh -m "automated_" -e -v 3 -A "tank/remote" "root@far:backup/arc" 2>"$e"; rc=$?; cat "$e" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "arc hourly backup (remote)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"
59 * * * * e=$(mktemp); /REPO/snapsend.sh -m "automated_" -e -v 3 "tank/local" "backup/local" 2>"$e"; rc=$?; cat "$e" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "arc hourly backup (local)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"
59 * * * * e=$(mktemp); /REPO/snapsend.sh -m "automated_" -e -A -v 3 "tank/already" "root@far:backup/arc" 2>"$e"; rc=$?; cat "$e" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "arc hourly backup (already)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"
59 * * * * e=$(mktemp); /REPO/snapsend.sh -m "automated_" -e -z -v 3 "tank/explicit-z" "root@far:backup/arc" 2>"$e"; rc=$?; cat "$e" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "arc hourly backup (explicit z)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"
59 * * * * e=$(mktemp); /REPO/snapsend.sh -m "automated_" -e -v 3 "tank/optedout" "root@far:backup/arc" 2>"$e"; rc=$?; cat "$e" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "arc hourly backup (opted out)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"
59 * * * * e=$(mktemp); /REPO/snapsend.sh -m "automated_" -A "tank/bare" "root@far:backup/arc" 2>"$e"; rc=$?; cat "$e" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "arc hourly backup (bare)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"
59 * * * * e=$(mktemp); /REPO/snapsend.sh -m "automated_" -e -u -A "tank/tiered" "root@far:backup/arc" 2>"$e"; rc=$?; cat "$e" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "arc hourly backup (tiered)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"
# END zfs-backup-managed
