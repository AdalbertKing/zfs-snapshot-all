# BEGIN zfs-backup-managed (generated by gen-cron.sh -- do not hand-edit, re-run gen-cron.sh instead)
37 * * * * echo "$(date -Is) ZFS-JOB BEGIN testhost hourly backup (x)" >>/LOG; e=$(mktemp 2>/dev/null) || e=/LOG.err.$$; /REPO/snapsend.sh -m "automated_hourly_" "tank/x" "backup/store" 2>"$e"; rc=$?; cat "$e" >>/LOG; echo "$(date -Is) ZFS-JOB END testhost hourly backup (x) rc=$rc" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "testhost hourly backup (x)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"
0 3 * * 0 echo "$(date -Is) ZFS-JOB BEGIN testhost weekly backup (x)" >>/LOG; e=$(mktemp 2>/dev/null) || e=/LOG.err.$$; /REPO/snapsend.sh -m "automated_weekly_" "tank/x" "backup/store" 2>"$e"; rc=$?; cat "$e" >>/LOG; echo "$(date -Is) ZFS-JOB END testhost weekly backup (x) rc=$rc" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "testhost weekly backup (x)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"
20 3 1 * * echo "$(date -Is) ZFS-JOB BEGIN testhost monthly backup (x)" >>/LOG; e=$(mktemp 2>/dev/null) || e=/LOG.err.$$; /REPO/snapsend.sh -m "automated_monthly_" "tank/x" "backup/store" 2>"$e"; rc=$?; cat "$e" >>/LOG; echo "$(date -Is) ZFS-JOB END testhost monthly backup (x) rc=$rc" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "testhost monthly backup (x)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"
0 3 * * 1-5 echo "$(date -Is) ZFS-JOB BEGIN testhost workday backup (x)" >>/LOG; e=$(mktemp 2>/dev/null) || e=/LOG.err.$$; /REPO/snapsend.sh -m "automated_workday_" "tank/x" "backup/store" 2>"$e"; rc=$?; cat "$e" >>/LOG; echo "$(date -Is) ZFS-JOB END testhost workday backup (x) rc=$rc" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "testhost workday backup (x)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"

51 * * * * echo "$(date -Is) ZFS-JOB BEGIN testhost hourly prune (x)" >>/LOG; e=$(mktemp 2>/dev/null) || e=/LOG.err.$$; /REPO/delsnaps.sh "tank/x" "automated_hourly" -H24 2>"$e"; rc=$?; cat "$e" >>/LOG; echo "$(date -Is) ZFS-JOB END testhost hourly prune (x) rc=$rc" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "testhost hourly prune (x)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"
40 3 * * 0 echo "$(date -Is) ZFS-JOB BEGIN testhost weekly prune (x)" >>/LOG; e=$(mktemp 2>/dev/null) || e=/LOG.err.$$; /REPO/delsnaps.sh "tank/x" "automated_weekly" -W4 2>"$e"; rc=$?; cat "$e" >>/LOG; echo "$(date -Is) ZFS-JOB END testhost weekly prune (x) rc=$rc" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "testhost weekly prune (x)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"
50 3 1 * * echo "$(date -Is) ZFS-JOB BEGIN testhost monthly prune (x)" >>/LOG; e=$(mktemp 2>/dev/null) || e=/LOG.err.$$; /REPO/delsnaps.sh "tank/x" "automated_monthly" -M6 2>"$e"; rc=$?; cat "$e" >>/LOG; echo "$(date -Is) ZFS-JOB END testhost monthly prune (x) rc=$rc" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "testhost monthly prune (x)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"
40 3 * * 1-5 echo "$(date -Is) ZFS-JOB BEGIN testhost workday prune (x)" >>/LOG; e=$(mktemp 2>/dev/null) || e=/LOG.err.$$; /REPO/delsnaps.sh "tank/x" "automated_workday" -D10 2>"$e"; rc=$?; cat "$e" >>/LOG; echo "$(date -Is) ZFS-JOB END testhost workday prune (x) rc=$rc" >>/LOG; [ $rc -ne 0 ] && /NOTIFY "testhost workday prune (x)" "$(tail -n 8 "$e")" 2>>/LOG; rm -f "$e"

*/15 * * * * d=$(/REPO/check-snap-age.sh "tank/x" "automated_hourly" 90m 3h 2>&1); rc=$?; [ -n "$d" ] && echo "$d" >>/LOG; [ $rc -eq 1 ] && /WARN "testhost hourly getting stale (x)" "$d" 2>>/LOG; [ $rc -eq 2 ] && /NOTIFY "testhost hourly stale (x)" "$d" 2>>/LOG; [ $rc -ge 3 ] && /NOTIFY "testhost hourly monitor BROKEN (x)" "$d" 2>>/LOG
*/15 * * * * d=$(/REPO/check-snap-age.sh "tank/x" "automated_weekly" 9d 12d 2>&1); rc=$?; [ -n "$d" ] && echo "$d" >>/LOG; [ $rc -eq 1 ] && /WARN "testhost weekly getting stale (x)" "$d" 2>>/LOG; [ $rc -eq 2 ] && /NOTIFY "testhost weekly stale (x)" "$d" 2>>/LOG; [ $rc -ge 3 ] && /NOTIFY "testhost weekly monitor BROKEN (x)" "$d" 2>>/LOG
*/15 * * * * d=$(/REPO/check-snap-age.sh "tank/x" "automated_monthly" 33d 40d 2>&1); rc=$?; [ -n "$d" ] && echo "$d" >>/LOG; [ $rc -eq 1 ] && /WARN "testhost monthly getting stale (x)" "$d" 2>>/LOG; [ $rc -eq 2 ] && /NOTIFY "testhost monthly stale (x)" "$d" 2>>/LOG; [ $rc -ge 3 ] && /NOTIFY "testhost monthly monitor BROKEN (x)" "$d" 2>>/LOG
*/15 * * * * d=$(/REPO/check-snap-age.sh "tank/x" "automated_workday" 80h 96h 2>&1); rc=$?; [ -n "$d" ] && echo "$d" >>/LOG; [ $rc -eq 1 ] && /WARN "testhost workday getting stale (x)" "$d" 2>>/LOG; [ $rc -eq 2 ] && /NOTIFY "testhost workday stale (x)" "$d" 2>>/LOG; [ $rc -ge 3 ] && /NOTIFY "testhost workday monitor BROKEN (x)" "$d" 2>>/LOG
# END zfs-backup-managed
