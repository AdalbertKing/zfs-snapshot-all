# BEGIN zfs-backup-managed (generated by gen-cron.sh -- do not hand-edit, re-run gen-cron.sh instead)
37 * * * * /REPO/zfs-job.sh "testhost hourly backup (x)" --log=/LOG --notify=/NOTIFY --detail=8 -- /REPO/snapsend.sh -m "automated_hourly_" "tank/x" "backup/store"
0 3 * * 0 /REPO/zfs-job.sh "testhost weekly backup (x)" --log=/LOG --notify=/NOTIFY --detail=8 -- /REPO/snapsend.sh -m "automated_weekly_" "tank/x" "backup/store"
20 3 1 * * /REPO/zfs-job.sh "testhost monthly backup (x)" --log=/LOG --notify=/NOTIFY --detail=8 -- /REPO/snapsend.sh -m "automated_monthly_" "tank/x" "backup/store"
0 3 * * 1-5 /REPO/zfs-job.sh "testhost workday backup (x)" --log=/LOG --notify=/NOTIFY --detail=8 -- /REPO/snapsend.sh -m "automated_workday_" "tank/x" "backup/store"

51 * * * * /REPO/zfs-job.sh "testhost hourly prune (x)" --log=/LOG --notify=/NOTIFY --detail=8 -- /REPO/delsnaps.sh "tank/x" "automated_hourly" -H24
40 3 * * 0 /REPO/zfs-job.sh "testhost weekly prune (x)" --log=/LOG --notify=/NOTIFY --detail=8 -- /REPO/delsnaps.sh "tank/x" "automated_weekly" -W4
50 3 1 * * /REPO/zfs-job.sh "testhost monthly prune (x)" --log=/LOG --notify=/NOTIFY --detail=8 -- /REPO/delsnaps.sh "tank/x" "automated_monthly" -M6
40 3 * * 1-5 /REPO/zfs-job.sh "testhost workday prune (x)" --log=/LOG --notify=/NOTIFY --detail=8 -- /REPO/delsnaps.sh "tank/x" "automated_workday" -D10

*/15 * * * * d=$(/REPO/check-snap-age.sh "tank/x" "automated_hourly" 90m 3h 2>&1); rc=$?; [ -n "$d" ] && echo "$d" >>/LOG; [ $rc -eq 1 ] && /WARN "testhost hourly getting stale (x)" "$d" 2>>/LOG; [ $rc -eq 2 ] && /NOTIFY "testhost hourly stale (x)" "$d" 2>>/LOG; [ $rc -ge 3 ] && /NOTIFY "testhost hourly monitor BROKEN (x)" "$d" 2>>/LOG
*/15 * * * * d=$(/REPO/check-snap-age.sh "tank/x" "automated_weekly" 9d 12d 2>&1); rc=$?; [ -n "$d" ] && echo "$d" >>/LOG; [ $rc -eq 1 ] && /WARN "testhost weekly getting stale (x)" "$d" 2>>/LOG; [ $rc -eq 2 ] && /NOTIFY "testhost weekly stale (x)" "$d" 2>>/LOG; [ $rc -ge 3 ] && /NOTIFY "testhost weekly monitor BROKEN (x)" "$d" 2>>/LOG
*/15 * * * * d=$(/REPO/check-snap-age.sh "tank/x" "automated_monthly" 33d 40d 2>&1); rc=$?; [ -n "$d" ] && echo "$d" >>/LOG; [ $rc -eq 1 ] && /WARN "testhost monthly getting stale (x)" "$d" 2>>/LOG; [ $rc -eq 2 ] && /NOTIFY "testhost monthly stale (x)" "$d" 2>>/LOG; [ $rc -ge 3 ] && /NOTIFY "testhost monthly monitor BROKEN (x)" "$d" 2>>/LOG
*/15 * * * * d=$(/REPO/check-snap-age.sh "tank/x" "automated_workday" 80h 96h 2>&1); rc=$?; [ -n "$d" ] && echo "$d" >>/LOG; [ $rc -eq 1 ] && /WARN "testhost workday getting stale (x)" "$d" 2>>/LOG; [ $rc -eq 2 ] && /NOTIFY "testhost workday stale (x)" "$d" 2>>/LOG; [ $rc -ge 3 ] && /NOTIFY "testhost workday monitor BROKEN (x)" "$d" 2>>/LOG
# END zfs-backup-managed
