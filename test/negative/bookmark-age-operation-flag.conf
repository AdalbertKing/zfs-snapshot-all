# Same whitelist as retain= (basket A3), same reason: this string reaches
# delsnaps.sh verbatim, and "only -n is refused" left -F -- clear-cut -- valid
# in a field named 'age'.
[defaults]
	host_label = t
[prune-bookmarks:tank/vm1]
	schedule = 30 4 * * *
	age      = -F
