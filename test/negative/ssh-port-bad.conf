# ssh_port must be a plain port number.
[defaults]
	host_label = t
	dst        = backup@store:tank/x
[template:hourly]
	send_schedule = 0 * * * *
	prefix        = a_
[dataset:tank/x]
	use_template = hourly
	ssh_port     = notaport
