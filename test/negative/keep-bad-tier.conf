# keep= needs a known TIER_LETTER; a non-standard tier id must use retain= instead.
[defaults]
	host_label = t
[template:store_hourly]
	prune_schedule = 0 * * * *
	pattern        = automated_hourly
	keep           = 24
[dataset:tank/x]
	use_template = store_hourly
