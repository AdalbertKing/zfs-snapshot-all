# Directory-prefix resolution. "some/dir/" is not a real path in the repo, which
# is fine: --verify is not what this fixture exercises.

[file:some/dir/]
suites = gencron

[suite:gencron]
cmd   = ./test/run.sh
needs = nothing
