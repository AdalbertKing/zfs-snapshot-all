<!-- rev: REV-20260813-118 -->
<!-- verdict: CHANGES-REQUIRED -->
<!-- reviewed-implementation: b58bfaaf7f2ee5564409f2eab4ef40e43c0dde9e -->

# REV-20260813-118 — default recovery strategy preview

Severity: P1 safety / recovery preview
Routing: Claude
Response: `docs/internal/reviews/responses/REV-20260813-118.md`

## F1 — residual: `written=0` does not prove current live state

The new check correctly detects committed live changes and treats failed reads conservatively. But the live lab also proved that recent writes can still report `written=0` until the next TXG commit. Therefore `written=0` cannot by itself justify `NIC DO ZROBIENIA` or a no-blocker verdict.

Required correction:
- keep `written>0` as positive evidence of live changes;
- keep failed/non-numeric reads as unproven;
- treat `written=0` conservatively unless another genuinely read-only fact closes the TXG visibility gap;
- add a targeted regression for that zero/unproven branch;
- preserve the existing GUID-base, rollback, dirty and failed-read behavior.

The existing live transcript is sufficient evidence for the TXG limitation; no broader live campaign is required unless the replacement mechanism adds a new environment assumption.

## F2 — routing/CI bookkeeping

The reviewer-owned header syntax defect is now corrected. After F1 is fixed, regenerate routing/status and submit one exact implementation SHA with green dependency/impact CI.
