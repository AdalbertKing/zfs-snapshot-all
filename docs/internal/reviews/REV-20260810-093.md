<!-- rev: REV-20260810-093 -->
<!-- verdict: APPROVED -->
<!-- reviewed-implementation: 3495ec4d8706b22ae8f3cfdd199752285245213d -->

# REV-20260810-093 — Phase 3.5 generated-command contract is sound; targeted live proof completed

Status: APPROVED / CLOSED
Severity: P1 / Gate 3.5 blocker — resolved
Reviewed delivery: `3495ec4d8706b22ae8f3cfdd199752285245213d`
Response path: `docs/internal/reviews/responses/REV-20260810-093.md`
Closure path: `docs/internal/reviews/closures/REV-20260810-093.md`

## Independent review

The generator/config change itself is directionally and structurally correct.

- `prefix` and `gfs_pattern` may now be omitted across the inheritance chain, while present-but-blank remains rejected.
- The four create/existing x prefix/no-prefix generated-command cells are pinned by a targeted golden.
- Prefixless GFS preserves the empty positional PATTERN: generated shape is `delsnaps.sh -G "tank/store" "" -H24 ...`; `-G` does not slide into the pattern position.
- `pattern` for ordinary non-GFS prune remains unchanged.
- The implementation does not modify the frozen engines.
- The dependency-selected local cascade and negative control are appropriate filters for the generator-side change.

I do not find a production-code defect in `3495ec4d`.

## Live-host proof — completed

The previously missing evidence has now been supplied in an attended session on real ZFS, using the delegated `zfsbackup` identity that executes the generated jobs.

1. **prefixless create** — exact `snapsend.sh -m ""` runtime shape created a bare timestamp source snapshot and transferred the identical snapshot successfully.
2. **bare passive** — exact `-m "" -e` shape created no new source snapshot and selected/transferred the known newest existing snapshot; the older snapshot served as the requested negative control.
3. **prefixless GFS** — exact empty positional PATTERN runtime shape selected the all-name population without argument shifting; dry-run and real run agreed. Five same-hour snapshots correctly collapsed to the newest member of that hourly bucket; deterministic GFS tests cover the wider H/D/W/M bucket ladder.
4. Scratch ZFS state was removed afterward and root crontabs were reported byte-identical before/after.

The evidence is proportionate to the dependency under review; no broader live campaign is required.

## Final determination

REV-093 is APPROVED and CLOSED. Phase 3.5 production approval is granted. No engine thaw or production-code follow-up is required by this finding.
