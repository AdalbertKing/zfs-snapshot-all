<!-- rev: REV-20260811-102 -->
<!-- verdict: CHANGES-REQUIRED -->
<!-- reviewed-implementation: 2e4853d8eff104d1d9854cc3c58aa529248d1fb2 -->
<!-- severity: P1 -->
<!-- response: docs/internal/reviews/responses/REV-20260811-102.md -->

# REV-20260811-102 — default backup policy leaves source snapshots unbounded

## Scope

Default/high-level backup policy, both the Phase 5 local path and the existing remote PULL activation path. The defect is that the default currently bounds only the target/store side while tool-owned snapshots on the production source are unbounded.

Design discussion / owner direction:
`docs/discussions/SOURCE-TARGET-RETENTION-2026-08-11.md`

## Finding F1 — managed hourly snapshots on the source have no default prune

Fresh originally reviewed `main`: `542351826dfeede59ddbaf57851e17684853c127`.

The current built-in `default` profile renders:

- `dataset.inc` -> `use_template = standard_hourly`;
- `standard_hourly` -> `send_schedule` + `prefix = automated_hourly_`, but **no `prune_schedule` / `retain`**;
- `prune.inc` -> `keep_hourly,keep_daily,keep_weekly,keep_monthly` with GFS `-H24 -D7 -W4 -M12`.

That target-retention fragment is emitted only into standalone `[prune:<target>]` sections. In the local Phase 5 planner the generated `[dataset:<source>]` gets `standard_hourly`, while the GFS fragment is emitted as `[prune:<target>]`. In remote PULL, `emit_client_sections()` writes `[dataset:<local-target-path>]` with `src = account@host:<source>` and again writes the GFS `[prune:]` over the **local collector target**. There is no generated source-side remote `[prune:<host:source>]` equivalent.

`gen-cron.sh` only self-prunes a `[dataset:]` when its resolved template carries a prune schedule. `snapget.sh`/`snapsend.sh` do not perform an implicit source snapshot destroy after transfer. Therefore a default hourly job creates `automated_hourly_*` snapshots continuously on the source and, absent a hand-written/external cleanup rule, never removes them.

This is not merely cosmetic. A source pool with little free space can fill because the backup product itself accumulates its managed snapshots. That can impact the production workload the backup is supposed to protect.

The repository already documents the correct underlying model in `docs/examples/short-local-long-store.conf`: source retention and store retention are independent policies. The high-level default currently fails to provide any bounded source policy at all.

## Owner direction — one scheduler, two independent policies

Owner decisions recorded 2026-08-11:

1. **The collector (`pve1`) owns the cron and CONFIG for source retention as well as transfer/store retention.** A remote source must not acquire a second autonomous scheduler merely to clean these snapshots.
2. **The default preset should initially propose the SAME retention values for source and target/store.** This is the zero-surprise safe starting point: both sides are bounded and the operator sees one familiar policy rather than a hidden asymmetric default.
3. Equality is only a CREATE-time proposal. Once rendered, CONFIG v4 must hold **two separate native policies** that the administrator can edit independently before acceptance and later by normal CONFIG editing. There is no runtime linkage, inheritance, drift authority or automatic re-synchronisation between them.

For the current `default`, the simplest baseline proposal is therefore the same current ladder on both sides (24 hourly / 7 daily / 4 weekly / 12 monthly), unless the owner later chooses different default numbers. An administrator may then deliberately shorten the source or lengthen the store in the candidate CONFIG before install.

This does **not** turn source and target into one policy. It means the preset copies the same initial values into two independently owned CONFIG rules.

For a remote PULL relationship the collector schedules the source-prune operation and executes it on the source through the relationship's existing SSH / authorization boundary:

`collector cron + canonical CONFIG -> remote source prune over SSH`

not:

`collector schedules backup` + `source has a separate local cleanup cron/config`.

CONFIG v4/gen-cron already supports remote standalone `[prune:<host:dataset>]` with `ssh_flags`; prefer composing that existing primitive rather than inventing another retention language or scheduler.

For local source -> local target the same ownership rule is trivial: the one local CONFIG and cron generate both source-prune and target-prune jobs.

## Required correction

Preserve the architecture: **source retention and target/store retention are separate policies in native CONFIG v4.**

The default high-level CREATE path must:

1. propose bounded source retention and bounded target/store retention;
2. initialize both sides from the same preset retention values;
3. render them as separate CONFIG rules, so changing one does not change the other;
4. expose both clearly in the candidate/preview before install;
5. after any operator edit, revalidate the candidate and show the final CONFIG + cron diff before confirmation;
6. after installation, treat CONFIG v4 as runtime truth and never re-couple the two through the profile.

The implementation must cover both local source -> local target (`snapsend`) and remote source -> collector target (`snapget` PULL), where source cleanup is a collector-scheduled remote prune over the existing relationship SSH/grant boundary.

Only tool-owned snapshot patterns may be affected; manual/foreign snapshots must remain outside this cleanup rule. Do not invent profile inheritance, source-vs-target profile variants, a second scheduler, or a new retention framework.

## Timing / phase gate

REV-102 is a prerequisite to the Phase 5 transactional installer. Correct and prove source retention before the installer can commit/activate the one-command workflow.

Existing relationships must not be silently repaired by profile reapplication; they need the smallest explicit audit/previewed migration that adds only missing source-retention policy and preserves native CONFIG truth.

## Incremental-base safety

Source retention must not destroy transfer continuity accidentally. Incremental continuity may only be claimed when a usable ordinary common snapshot or proven usable bookmark remains. Bookmark support is conditional today (non-recursive fallback only, refresh best-effort); equal default retention reduces risk but does not prove continuity through every failure sequence.

Required live/negative evidence includes bookmark unavailable/failed, later snapshot creation/prune, recursive behavior, and a successful incremental-after-prune case where continuity is expected.

## Reviewer follow-up — partial local implementation at `2e4853d8...`

The local PUSH partial correctly adds separate source and target `[prune:]` sections initialized from the same profile ladder, and the preview exposes them separately. However it introduces a destructive scope mismatch that must be fixed before this partial can be accepted.

### F2 — source prune is recursive even when the backup source is non-recursive

`profiles/default/dataset.inc` deliberately emits no recursion field, so CONFIG v4 default semantics make `[dataset:rpool/data]` **non-recursive**: only the named dataset is sent.

The partial implementation nevertheless emits, for every source root:

```ini
[prune:rpool/data]
    ...
    recursive = yes
```

which gen-cron renders as `delsnaps.sh -R` over the whole source subtree. The new regression test explicitly expects that `-R` shape.

Concrete false-safety case:

- requested source WHAT = `rpool/data` only, non-recursive;
- child `rpool/data/vm-101` is outside this job's transfer scope and may carry `automated_hourly_` snapshots from another/manual policy;
- this job's source prune walks recursively into `vm-101` and can delete those matching snapshots even though this relationship never backs that child up.

Pattern ownership alone is not enough: the product's own `automated_` prefix can exist on descendants owned by another task. Retention scope must not be broader than the source coverage this relationship owns.

Required correction:

- source-prune scope must follow the effective source coverage/recursion contract, not be hard-coded recursive;
- default non-recursive source must prune only the named source dataset;
- flat/atomic cases need a deliberate mapping consistent with their actual source coverage, not a blanket `recursive=yes` shortcut;
- add a discriminating negative control with a child dataset carrying matching tool-owned snapshots outside the relationship coverage and prove they survive;
- keep target recursive retention independent; this finding is only about SOURCE ownership scope.

## Required evidence before closure

At minimum:

- deterministic generation tests for separate bounded SOURCE and TARGET policies initialized from the same preset values;
- source-prune scope matches effective source coverage/recursion;
- child/out-of-coverage matching snapshots survive;
- manual/non-matching source snapshots survive;
- target-only old snapshots survive source prune;
- remote PULL runs source prune under the actual delegated identity and pinned SSH path, with a negative control for missing/revoked `destroy` and no speculative privilege widening;
- bookmark-unavailable/failure and recursive cases are explicitly proven or safely refused/degraded;
- real ZFS local + remote proof including incremental-after-prune where guaranteed;
- explicit migration/audit behavior for existing CONFIGs;
- dependency-selected cascade + `./test/impact.sh --verify`.

## Questions for Claude in the response

1. What exact native CONFIG sections represent SOURCE and TARGET retention for local PUSH and remote PULL?
2. How does source-prune recursion/scope exactly follow the relationship's source coverage?
3. How does remote source prune reuse the relationship SSH key, host-key pinning and exact scope?
4. Does the current source-side grant already include precisely the destroy capability required; if so, how is that verified fail-closed without widening it?
5. What is the safe behavior when no usable bookmark/common snapshot exists, including recursive mode?
6. What is the smallest explicit migration path for existing CONFIGs lacking source retention?
7. How is the candidate edited, revalidated and re-previewed before commit without another representation?

## Verdict

CHANGES REQUIRED (P1). Partial implementation `2e4853d8eff104d1d9854cc3c58aa529248d1fb2` is reviewed but not accepted. REV-102 stays OPEN to Claude; Phase 5 transactional installation remains blocked.
