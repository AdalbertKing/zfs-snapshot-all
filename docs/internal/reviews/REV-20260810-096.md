<!-- rev: REV-20260810-096 -->
<!-- verdict: APPROVED -->
<!-- reviewed-implementation: ac6fe3119412a684edf483caa0895fd2bb1e5883 -->
<!-- supersedes: - -->
<!-- reviewed-by: ac6fe3119412a684edf483caa0895fd2bb1e5883 REV-20260810-096 -->

# REV-20260810-096 — configexamples does not require semantic coverage for newly added runnable examples

Review time: 2026-08-10 22:07 CEST
Reviewed implementation: `ac6fe3119412a684edf483caa0895fd2bb1e5883`

## Verdict

**APPROVED — the submitted meta-guard makes semantic-coverage registration total over the runnable example set and satisfies the requested test-hygiene property.**

REV-094 remains closed for the four examples that triggered it. REV-096 addressed the remaining durability gap: future runnable examples may no longer appear or disappear without the semantic-coverage registry noticing.

The implementation uses the explicitly accepted minimal shape from this review: an inline `COVERED` registry plus a bidirectional comparison against `docs/examples/*.conf`.

Independent inspection of exact implementation `ac6fe3119412a684edf483caa0895fd2bb1e5883` confirms:

1. every current runnable example is still rendered by the real `gen-cron.sh` and retains the existing Layer-2 semantic assertions;
2. an unregistered `.conf` is reported as `uncovered:<base>`;
3. a registry entry whose `.conf` is missing is reported as `stale:<base>`;
4. `coverage/total` requires empty mismatch output on the real example tree;
5. deterministic fixture controls exercise both uncovered and stale directions without mutating the committed real example tree;
6. the implementation touches only `test/configexamples/run.sh` plus status documentation — no production code, host state, ZFS, SSH, cron, grants or account boundary changed.

The exact repository state at the reviewed implementation contains four runnable examples — `independent-tiers`, `selective-quiesce`, `short-local-long-store`, and `manual-prune` — matching the four `COVERED` entries exactly.

Claude reports `test/configexamples/run.sh` 24/24 and `./test/impact.sh --verify` green, with a real-tree proof that a transient fifth unregistered example makes `coverage/total` fail. The code-level negative controls are discriminating and match the acceptance property.

No live ZFS/SSH/UID/crontab/grant/deploy proof is required for this test-only change.

## Phase impact

REV-096 is satisfied and may close. Phase 6 runnable-example hygiene no longer has this blocker. This review does not alter the already-closed Phase 4/Gate 4 and does not block Phase 5.

Response path: `docs/internal/reviews/responses/REV-20260810-096.md`
