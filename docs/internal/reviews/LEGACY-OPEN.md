# Pre-V2 reviews still awaiting a verdict

These reviews predate REVIEW PROTOCOL V2. They carry no machine headers, so the
generated `REVIEW_LEDGER.md` cannot show them -- and adding headers to
reviewer-authored files would be the implementer editing reviewer artifacts
(Principle 3). They are listed here so the fact that a verdict is owed is not
lost with the old table.

This file shrinks to nothing and is deleted as each one closes. Nothing new is
ever added: every review from V2 onward lives only in the ledger.

| # | Thread | Whose move |
|---|---|---|
| 21b | REV-054 — implemented | reviewer: verdict |
| 21e | REV-055 — CHANGES REQUIRED, already fixed | reviewer: verdict |
| 21f | Thread #18 recurred | reviewer: whether #18 needs a stronger mechanism than intent |
| 21d | `cron2conf.sh` cannot parse `snapget` lines | reviewer: is this worth a review of its own |
| 21 | Recursion is three decisions, not one | — |
| 25 | REV-057 — migration path | reviewer: verdict |
| 37 | REV-061 — both findings implemented | reviewer: verdict |
| 31 | `-r -R` together is not refused | reviewer: fold into the CLI slice |
| 23 | Monitor alerts on a dataset too new to be late | reviewer: settle wording, UNKNOWN-on-unreadable-creation, and the test home |
