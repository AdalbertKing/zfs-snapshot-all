<!-- rev: REV-20260829-125 -->
<!-- response-status: IMPLEMENTED -->
<!-- implementation: - -->

# Response to REV-20260829-125

Reviewed delivery: `cf668df5486342ad9c3604cef801ff668c3b5ce6`.
Canonical descendant the finding was reproduced against:
`e33b6670739c9b1f439bbfe1f3038ff392424509` — confirmed identical to the
`test/reviewctl.sh` this branch started from (same md5).
Implementation: stamped after the merge boundary — `reviewctl` will not accept a
SHA unreachable from canonical `main`, and an open PR is not a submission.

## F1 — IMPLEMENTED

### Analysis

Accepted in full, including the part that is easy to argue away. The command did
return nonzero, and that is not the property. The property is that a refusal
leaves the reviewer's fact byte-identical, because the next run reads the
artifact, not the exit code. A transaction that mutates and then reports failure
without restoring has done something worse than not existing: it looks like a
guard in review.

The mechanism is exactly as described. `tx_guard` appended the path to
`TX_FILES` and then ran `mkdir -p`, `cp -p`, or the absent-list append with no
result checked. A failed snapshot therefore produced an entry in `TX_FILES` with
no file under `present/` and no line under `absent/` — and `tx_restore`'s two
branches are keyed on exactly those two artifacts, so neither matched. It
silently skipped the path, `tx_cleanup` removed the only remaining copy, and the
verdict stayed flipped.

`tx_restore` ignoring its own `cp`/`rm` results and `tx_cleanup` deleting
unconditionally is the same failure on the other side of the mutation boundary,
and I am treating it as one finding rather than two because a fix to either half
alone still loses data.

### Implemented change

`test/reviewctl.sh`.

**Snapshot is a hard precondition.** Every `mkdir -p`, `cp -p` and absent-list
append is checked, and any failure calls `tx_die` *before* the writer touches a
live artifact. The copy is additionally verified to exist afterwards, because a
`cp` that reports success and produces nothing would otherwise reintroduce the
same hole one layer down. A path joins `TX_FILES` only **after** its snapshot
exists, so `tx_restore`'s invariant now holds by construction: every entry has
either a `present/` copy or an `absent/` record.

**Restoration checks every operation.** `tx_restore` returns nonzero if any path
could not be put back and prints `UNRECOVERED: <path>` for each one, naming the
backup it should be restored from.

**The backup survives a failed restoration.** `tx_cleanup` deletes the
transaction directory only when it is provably no longer needed. On a nonzero
`tx_restore` it keeps it and says where it is, because at that moment it holds
the only intact copy of the reviewer's facts.

### Evidence

- `./test/reviewctl/run.sh` — **86/0**, with two new sections.
- **X1 — the snapshot cannot be taken.** A PATH stub fails only `cp` calls whose
  destination is inside the transaction `present/` directory, exactly as the
  reviewer reproduced it: real transaction, real writer, real generator, one
  system call broken. Each world also carries an orphan response so the
  generator *after* the mutation refuses — without it neither control would
  reach the code it aims at. Asserts: nonzero exit, the role artifact
  byte-identical, still `CHANGES-REQUIRED`, and the refusal names the snapshot
  as the reason.
- **X2 — the snapshot was taken but restoring it fails.** The stub fails `cp`
  calls whose *source* is inside `present/`. Asserts: nonzero exit,
  `UNRECOVERED:` naming the path, the message saying the backup was kept, and
  the directory actually existing at the path it named.
- **Negative control against canonical `e33b667`.** Run by replacing
  `test/reviewctl.sh` in the working tree with that version rather than pointing
  `CTL` at a copy elsewhere: the script derives `REPO` from its own location, so
  a copy under `/tmp` fails 39 unrelated cases and proves nothing. That first
  attempt is recorded here because a contaminated control is worse than none —
  it produces a large red number that looks like evidence.

  With the swap, the result is exactly six failures and nothing else:

  ```
  FAIL X1: THE ROLE ARTIFACT IS BYTE-IDENTICAL AFTER THE REFUSAL
  FAIL X1: ...still CHANGES-REQUIRED, not APPROVED
  FAIL X1: ...and says the snapshot is why
  FAIL X2: IT NAMES THE PATH IT COULD NOT PUT BACK
  FAIL X2: ...and says the backup was kept
  FAIL X2: THE BACKUP DIRECTORY ACTUALLY SURVIVES, AT THE PATH IT NAMED
  PASS=80 FAIL=6
  ```

  The first two are the finding itself: on canonical `main` the artifact really
  does change while the command refuses.

### Criterion 5 — nothing already correct was disturbed

The separate approve/close publications, the exact current-implementation check,
the published-parent checks, the delivery acknowledgement and the idempotent
replay verification are untouched; their cases pass unchanged in the 86/0, and
the six failures above are the only difference between the two builds.

### Remaining risk

The reviewer's own standing caveat still holds and this change does nothing to
alter it: none of this gates canonical `main`. A caller can still hand-edit and
push. That gate is GitHub branch protection, which the Owner has yet to enable,
and until it is measured the invariant must not be described as enforced.

## Suites and CI

`./test/reviewctl/run.sh` **86/0**. `./test/impact.sh --verify` green.
Exact-head CI is reported with the submission.
