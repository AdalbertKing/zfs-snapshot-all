<!-- rev: REV-20260808-073 -->
<!-- response-status: IMPLEMENTED -->
<!-- implementation: 12541c7dd9a04bf7a637cc3ac098c8c99a2a786f -->

# Response to REV-20260808-073

## F1 — ACCEPTED, corrected before any implementation

The wording was wrong and the correction is welcome. `PLAN-2026-08-07.md` said
the profile "trzyma **selektor**" with `effective = selektor ∩ grant`, which
collapses **what** is backed up and **how** into one layer.

It also contradicted documents this project had already agreed:
`DEPLOY-UX-AGREED-POSITION.md` (the collector obtains available datasets from
pve2, the deployment selects, pve1 builds the local CONFIG v4) and
`AI-BRAINSTORM-SYNTHESIS-2026-08-07.md` (profile policy is reusable
how-to-back-up intent, explicitly excluding topology and grant authority). I
wrote the plan text and did not reconcile it with either.

## What changed, against your acceptance criteria

1. **Selector removed from the profile.** The Stage 5 section carries the
   ownership diagram: deploy/relationship discovers, selects, and owns
   source→target mapping and grant; the profile owns cadence, retention/GFS,
   recursion, monitoring, quiesce and autotune.
2. **Discovery and selection named as deploy's**, in the owner's own words,
   quoted in the document.
3. **Profile scope stated as policy only**, limited to native CONFIG v4 fields.
4. **The existing ban is preserved verbatim**: `src`, `dst`, topology, grants,
   endpoint/key material, raw flags.
5. **Both shapes shown**: a `default` profile with no dataset names, and the
   per-dataset sections a deployment adds that reference it.
6. **No runtime work started**, and none will be on the old wording. The
   "gotowe gdy" list now also requires that a profile containing a dataset name
   is **rejected**, so the boundary becomes testable rather than aspirational.

One point I made explicit beyond the wording: refusing to exceed the grant stays
a refusal that names the datasets, but it is decided by the **relationship**,
not the profile — otherwise the profile would still be the scope authority by
another route.

## Evidence

- commit: `12541c7dd9a04bf7a637cc3ac098c8c99a2a786f`
- documentation only; no code touched
- `./test/profiles/run.sh` **22/22**, unchanged, including its rejection of
  relationship-owned fields
- `./test/impact.sh --verify` clean
- no ZFS or live-host run, per your proof boundary

## Remaining risk

Stage 4's `--reconcile` compares what exists against what the config covers. It
does not yet distinguish **discovered / selected / granted / effective** as
separate scopes, which is what would let it report "granted but not selected".
Your note says Stage 4 may compare them without owning the selector; that
comparison is not built, and I am not treating today's coverage check as
equivalent to it.
