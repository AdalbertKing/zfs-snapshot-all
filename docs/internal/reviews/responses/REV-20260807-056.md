# Response to REV-20260807-056 — monitor age from dataset creation

**ACCEPTED**, implemented in `1124933`. All six contract points and every
required test case.

## Contract

1. **Uniform age rule — IMPLEMENTED.** No matching snapshot, dataset exists,
   has at least one snapshot → age from `creation`, same WARN/CRIT ladder,
   explicit and `-R` discovered alike. No config field, no inferred recursion.
2. **Container leniency — UNCHANGED.** A non-explicit `-R` descendant with no
   snapshots at all still SKIPs; pinned by B3, which also asserts the SKIP line
   itself so a future change cannot quietly turn empty nodes into monitored
   leaves.
3. **Provenance — IMPLEMENTED.** Lines caused by dataset age read
   `-- no snapshot found matching this pattern; age measured from dataset
   creation`. Under `-v` the below-warning case emits `OK` with the same
   clause; without `-v`, cron stays silent (E1 asserts empty output, not just
   rc=0).
4. **Unreadable timestamps — UNKNOWN, exit 3.** Never fed to arithmetic.
5. **The pre-existing matching-snapshot path — FIXED in the same change.** Both
   reads now go through one validated helper, `creation_epoch()`, which returns
   failure on an empty, non-numeric or `-` value. The old inline
   `$((NOW - $(zfs get ...)))` read those as 0, so a failed read became an age
   of the entire Unix epoch and a CRITICAL with a measurement nobody made.
   C2 pins both halves: UNKNOWN, and *not* CRITICAL.
6. **No production surface — CONFIRMED.** `gen-cron.sh`, `cron2conf.sh`,
   generated crontabs and deployed configs are untouched. The only runtime file
   changed is `check-snap-age.sh`; the rest of the commit is `test/monitor`,
   `test/deps.conf` and `PROJECT_STATUS.md`.

One thing not in the contract, added because the same branch invites it: a
future-dated `creation` (clock skew, a pool imported from a host running ahead)
clamps to age 0 rather than going negative. A negative age is not evidence of
freshness, but it would have sorted like it.

## Tests

`test/monitor`, **24/24**. Deterministic as required: `zfs` is a PATH stub
answering from files, and every timestamp is an offset from one captured `NOW`.
No root, no ZFS, no sleeping to age a snapshot — a suite that races the clock
gets muted on the first slow machine.

Every case the review listed:

| required | case |
|---|---|
| below WARN → rc 0, silent, `OK` + provenance under `-v` | A1, E1 |
| between WARN/CRIT → rc 1 + provenance | A2 |
| ≥ CRIT → rc 2 + provenance | A3 |
| explicit and discovered use the same ladder | B1, B2 |
| discovered node with no snapshots stays SKIP | B3 |
| unreadable dataset `creation` → UNKNOWN, no fabricated age | C1 |
| unreadable matching-snapshot `creation` → UNKNOWN | C2 |
| aggregate semantics across mixed results | D1, D2, D3 |
| negative control against the reviewed code | below |

Plus two the review did not ask for and I would not have trusted the change
without: **A4**, an old dataset with a *fresh* matching snapshot still reads OK
(the new rule must cover the gap in the old one, not replace it), and **B4**, an
explicit dataset with no snapshots at all is judged rather than skipped.

**Negative control** against `103cf09`: **14 fail, 10 pass**. The ten that pass
are precisely the behaviours this change does not alter — that is what makes
the fourteen mean something rather than proving the suite is simply
incompatible with the old file.

## Test-graph home

`check-snap-age.sh` had no suite. Its `deps.conf` note said it did not need
one: *"a thin `zfs list` + threshold parse, and its real contract is the exit
code, pinned by the gen-cron golden files."*

That note was wrong, and I replaced it rather than edited it. The goldens pin
the cron **line**, not what the script does when it runs, and both defects
closed here lived in exactly that gap — invisible to every suite that existed.
The new note says so, so the next person does not re-derive it.

`[suite:monitor]` declared, `check-snap-age.sh` now lists it, `impact.sh
--verify` clean, and both new suites added to `PROJECT_STATUS.md` (the impact
suite enforces that, and caught me).

## Evidence

- commit: `1124933`
- `test/monitor` **24/24**, `test/pairpause` 18/18, `test/impact` 21/21
- `./test/impact.sh --verify` — graph consistent with the tree
- runtime files changed outside `check-snap-age.sh` and its tests: **none**

## Not covered

`sudo ./test/scenarios/run.sh` is selected by the graph (it runs generated cron
lines verbatim and `check-snap-age.sh` changed) and needs root + ZFS, which
this workstation does not have. It exercises the monitor through the cron idiom
rather than the new branch, and the deterministic suite covers the branch
directly — but it has not been run for this change and I am not reporting it as
if it had.

Findings left **IMPLEMENTED**; closure is the reviewer's.
