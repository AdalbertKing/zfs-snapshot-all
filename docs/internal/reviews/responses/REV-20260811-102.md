<!-- rev: REV-20260811-102 -->
<!-- response-status: IMPLEMENTED -->
<!-- implementation: b9fcd402594ed2e989b773f437b513c4e759d8e2 -->

# Response to REV-20260811-102

## Sole remaining blocker — continuity after loss of the ordinary common base — IMPLEMENTED (evidence-only, no product-code change)

Ran the targeted real-ZFS runtime campaign the review asks for, 2026-08-12
11:37-11:48 CEST. **No code change was required and none was made**: the measured
behaviour already satisfies the acceptance property in every case, on both the
recursive and the non-recursive path, on both the PULL and the local PUSH engine.

### Environment

| role | host | account | checkout |
|---|---|---|---|
| collector / TARGET | pve1 (192.168.28.9) | `zfsbackup` (delegated) | `/home/zfsbackup/zfs-snapshot-all` @ `bb8449c` |
| SOURCE | pve2 (192.168.28.8) | `zfsbackup` (delegated) | `/home/zfsbackup/zfs-snapshot-all` @ `bb8449c` |

Both hosts `zfs-2.1.9-pve1`. Every transfer and every source prune ran as the
delegated `zfsbackup` account over the relationship's own SSH identity — not as
root — so this is the production shape, not a root-only approximation.

Throwaway datasets (all created for this campaign):

- `hdd/backuptest/rev102cont/nr` (pve2) -> `hdd/backuptest/rev102contbase` (pve1) — leg A
- `hdd/rev102nobm` (pve2) -> `hdd/backuptest/rev102nobmbase` (pve1) — leg A6, deliberately delegated **without** `bookmark`
- `hdd/backuptest/rev102rec` + `/c1` (pve2) -> `hdd/backuptest/rev102recbase` / `rev102recRbase` (pve1) — leg B
- `hdd/backuptest/rev102push` -> `hdd/backuptest/rev102pushbase` (both on pve2) — leg C

### A — non-recursive / bookmark path (PULL)

Transfer command (identical every run, only the flag under test varies):

```
snapget.sh -v 3 -m automated_hourly_ \
    zfsbackup@192.168.28.8:hdd/backuptest/rev102cont/nr hdd/backuptest/rev102contbase
```

Source prune command (collector-scheduled, delegated identity, exactly the shape
the package emits):

```
delsnaps.sh "zfsbackup@192.168.28.8:hdd/backuptest/rev102cont/nr" "automated_hourly_" -H1
```

| # | step | measured result |
|---|---|---|
| A1 | initial transfer | `Performing standard full pull`; `zfs send -c <snap>`; rc=0. Bookmark `#tgt-e25797d2` recorded on the source. |
| A2 | second transfer | `Found valid common snapshot`; `zfs send -c -I @11-37-42 @11-38-16`; **genuinely incremental**; recv ran **without** `-F` (`written@` = 0 gate). |
| A3 | source prune keep 1, then transfer | prune destroyed `@11-37-42` on the SOURCE only; TARGET kept both. Next transfer `-I @11-38-16 @11-39-07` — **incremental after source prune while a common base remains** (review item 4). TARGET history 3 snapshots. |
| A4 | destroy the ordinary common snapshot, bookmark still present | source pruned to `@11-40-00` only; bookmark GUID `4864933676394441219` == TARGET head GUID. Next transfer: `No common snapshot, but a bookmark still anchors an incremental: …#tgt-e25797d2`; `zfs send -c -i <bookmark> @11-40-02`; rc=0. **Real bookmark fallback exercised, incremental.** TARGET history 4 snapshots, nothing lost. |
| A5 | no common snapshot **and** no bookmark | `Refusing: '<target>' already exists and shares no common snapshot (by GUID) with '<source>' -- a full resend needs '-f', which destroys and recreates the target. That is a decision for a human to make explicitly, not this run's default.` **exit 1**, on **stderr**, inside the last 8 lines the cron wrapper mails. TARGET history unchanged (4 snapshots, same GUIDs). |
| A6 | bookmark creation/refresh **unavailable** | `hdd/rev102nobm` delegated `create,destroy,hold,mount,receive,release,send,snapshot` — no `bookmark`. Transfer succeeded (rc=0) and logged `cannot create bookmark 'hdd/rev102nobm#tgt-33ff7bbf': permission denied` + `Warning: failed to refresh bookmark … (non-fatal)`. No bookmark exists. Losing the base afterwards then produced the same explicit refusal, **exit 1**, TARGET intact. |
| A7 | the advertised remedy | explicit `-f`: `Force full pull activated (-f)` / `Destroying all snapshots and data on local target dataset` / full pull / rc=0. The escape hatch the refusal names is real, and it is loud about what it destroys. |

### B — recursive path (PULL, `-r`)

| # | step | measured result |
|---|---|---|
| B2 | initial `-r` transfer | `zfs send -c -R @11-43-27`; rc=0; parent + `c1` both received. |
| B3 | second `-r` transfer | `Found valid common snapshot`; `-R -I`; incremental; **zero bookmarks on the source** — confirming directly that no bookmark can rescue a `-r` relationship (the engine deliberately neither records nor consults one under `-r`). |
| B4 | last common source snapshot removed (recursive prune keep 1, parent and child), no bookmark possible | same explicit refusal, **exit 1**, on stderr. **TARGET tree fully intact**: parent and `c1`, both snapshots each, unchanged. |
| B5 | control: flat-recursive `-R` | the same source under `-R` **does** get per-dataset bookmarks (`rev102rec#tgt-77086bac`, `rev102rec/c1#tgt-551d72e7`). The missing insurance is specific to atomic `-r`, not to recursion as such. |

### C — local PUSH engine (`snapsend.sh`), same boundary

REV-102 steps 1-2 install source retention on local PUSH relationships too, and
that engine has no tool-level no-common-base guard — it builds the FULL send and
lets `zfs recv -F` decide. Measured rather than assumed:

| # | step | measured result |
|---|---|---|
| C2/C3 | full then incremental, local push as `zfsbackup` | rc=0; bookmark `#tgt-692647f0` recorded. |
| C4 | bookmark destroyed, common base pruned away | `zfs recv -F` **refused**: `cannot receive new filesystem stream: destination has snapshots (eg. …@11-44-59) / must destroy them to overwrite it`; `Transfer failed`; **exit 1**. **TARGET history intact, both snapshots, same GUIDs.** |

`-F` on a *new filesystem stream* does not overwrite a destination that holds
snapshots — ZFS itself is the backstop here, so the PUSH path cannot silently
discard TARGET history either. That was the one way this could have been unsafe,
and it is not.

### Answers to the review's five evidence items

1. **Non-recursive / bookmark path** — A1-A7. Incremental proven genuine, bookmark
   fallback proven real, bookmark-unavailable case proven, later source snapshot
   created and transferred at every stage.
2. **Recursive path** — B2-B4. Not a FULL resend: an **explicit refusal**, exit 1.
3. **TARGET preservation** — held in every single case: A3, A4, A5, A6, B4, C4. The
   only history loss in the whole campaign was A7, where a human typed `-f` and the
   tool announced the destruction first.
4. **Successful incremental-after-prune where continuity is claimed** — A3 (ordinary
   common snapshot) and A4 (bookmark). Both verified against the actual `zfs send`
   command line, not a log adjective.
5. **No-base degraded behaviour is explicit** — refusal text names the dataset pair,
   the cause (no common snapshot by GUID) and the remedy; it is written to stderr;
   the process exits 1; the generated cron wrapper turns a non-zero exit into
   `notify-fail.sh`. Verified by capturing the exact `tail -n 8 "$e"` the wrapper
   mails. There is no path on which incremental continuity is silently claimed.

### Applying the agreed decision rule

The rule offered two outcomes: allow + warning if the recursive no-base case
produces a controlled successful FULL, hard refuse if it is ambiguous or unsafe.
The measured behaviour is **neither** — it is already the strictest option:

- it does not attempt a FULL over live TARGET history at all (PULL refuses before
  sending; PUSH is refused by `zfs recv`);
- TARGET history survives;
- the boundary is explicit — non-zero exit plus a stderr message that the existing
  cron/alert path already mails.

So **no new mechanism is warranted, and no activation/migration hard-refuse guard
is needed**: the failure mode that guard would exist to prevent — source retention
silently degrading protection — does not occur. Adding a refusal at activation time
would only forbid configurations that the runtime already handles safely. No frozen
transfer engine was touched.

### Standing operational consequence — named, not hidden

After the base is lost the relationship **stops** until a human acts. Protection is
not silently lost, but it is not self-healing either: every subsequent run refuses
and mails. Three facts shape the real exposure, all measured above:

- an ordinary non-recursive relationship carries a bookmark, so it survives source
  retention outrunning the target (A4);
- an atomic `-r` relationship carries **no** bookmark and therefore has no
  insurance at all (B3) — flat `-R` does (B5);
- a delegated account without `bookmark` permission silently has no insurance
  either, at rc=0, visible only as a warning line in the cron log (A6).

Two things a reviewer may want to weigh (neither implemented here, neither claimed
as a defect): whether `deploy.sh` should verify the `bookmark` grant the way it
already verifies `destroy`, and whether the package should prefer `-R` over `-r`
for recursive relationships precisely because `-R` keeps the insurance. Both are
outside this finding's accepted scope, so I am flagging, not building.

Also observed, not a defect: each refused PULL run creates its source snapshot
before reaching the refusal, so repeated refusals leave untransferred snapshots on
the source (three of them here). They are bounded by the source retention that is
the subject of this very finding, and holds were correctly released — no hold leak.

### Evidence

- commands and outputs: exactly as tabulated above, run 2026-08-12 11:37-11:48 CEST
  against pve1/pve2 at `bb8449c`;
- product code changed: **none**;
- L0 (per REV-109, closed): `./test/zfsbackup/run.sh --section retention` on pve1 —
  **PASS=13 FAIL=0**;
- L1 and `./test/impact.sh --verify`: recorded below with this resubmission.

## F4 — ACCEPTED by reviewer (`8cb28f3`). ## F5 — ACCEPTED by reviewer (`8cb28f3`).

The reviewer accepted the narrow-retrofit (F4) and the direct `--apply` transaction
test (F5) at `8cb28f3`. No further change on those.

## F3 residual — IMPLEMENTED — bound THIS relationship's managed family, not any prune on the scope

ACCEPTED. `8cb28f3`'s `source_scope_is_bounded` matched **any** `delsnaps` line
naming the exact scope. That is stronger than a header but still false-green in two
valid CONFIGs the reviewer named: a `[prune-bookmarks:<scope>]` renders
`delsnaps -B … "scope" …` (deletes bookmarks, never `automated_hourly_*` snapshots),
and a `[prune:<scope>]` may prune an unrelated prefix — both satisfy the old grep
while this relationship's managed snapshots stay unbounded.

Fix: `source_scope_is_bounded` now discriminates the rendered command's snapshot-prune
semantics (still no second CONFIG parser — it reads gen-cron's own output):

- a bookmark cleanup (`delsnaps -B`) is excluded — it does not delete snapshots;
- the prune's pattern argument must COVER the managed source family: the managed
  prefix must begin with the pattern, so every `<managed-prefix>*` snapshot matches.
  The managed prefix is read back from the relationship's own `snapget -m` transfer
  line in the render (`managed_source_prefix_for_scope`), falling back to the
  high-level default `automated_hourly_`;
- the line must carry a finite count/GFS retention flag (`-H24`/`-c 24`/`-d 30`).

Negative controls (`test/zfsbackup` section 57): same scope + `prune-bookmarks` only
→ still reported missing; same scope + snapshot prune of an unrelated prefix → still
missing; same scope + managed-prefix bounded prune → bounded. Plus the earlier F3
controls (header that renders no delsnaps → missing).

## F4 — IMPLEMENTED — `--apply` adds only missing source retention, no topology repair

ACCEPTED. The first cut called `emit_client_sections … is_new=0` (the ordinary
reactivation path), which refreshes `[dataset:]` `src`/`flags` to the currently-loaded
endpoint and can move an existing source-prune endpoint — so a retention retrofit
could perform an unrelated topology refresh, especially when client state and the
installed CONFIG disagree.

Fix: `--apply` no longer calls `emit_client_sections`. A new NARROW emitter
(`emit_missing_source_prune`) appends ONLY the missing source `[prune:]` sections and
their `__src_` templates; existing `[dataset:]`, target prune, and existing bounded
source prunes are left byte-identical. The source scope is read from the INSTALLED
`[dataset:]` `src` (`installed_dataset_src`) — CONFIG is truth, not client state. If
the client's current endpoint DISAGREES with the installed `src`, the retrofit
REFUSES and tells the operator to reconcile (re-activate) first: no opportunistic
endpoint repair, and no grant-checking/pruning the wrong host.

Discriminating regression (section 57): a client whose current endpoint (10.9.9.9)
differs from the installed `src` (10.7.7.1) → `--apply` refuses, config byte-identical.
With agreeing endpoints → `--apply` adds a bounded source prune at the installed
endpoint and the `[dataset:]` `src` line is byte-identical (topology not rewritten).

## F5 — IMPLEMENTED — direct `--apply` transaction test

ACCEPTED. Section 57 now exercises the `--apply` orchestration end-to-end through a
REAL profile + REAL `gen-cron.sh` (grant gate and install stubbed): the success path
(workfile build → grant → gen-cron validate → preview → confirm → install → read-back)
installs a bounded source prune; the failure path (grant check returns non-zero)
installs nothing and leaves the config byte-identical.

## F2 — IMPLEMENTED (on main) — source-prune scope follows source coverage, not a blanket recursive

The step-2 emission first made the source `[prune:<root>]` `recursive = yes` while
the `[dataset:<root>]` is non-recursive, so `delsnaps -R` could walk into a child
like `<root>/vm-101` and destroy tool-owned `automated_` snapshots outside this
relationship's coverage. Corrected: the local-PUSH source prune omits `recursive`
(CONFIG default = no) — `delsnaps -G` without `-R`, exactly the named dataset. The
target prune stays recursive over the store; independence preserved.

Proven, real ZFS (2026-08-11, pve1 192.168.28.9, throwaway `hdd/backuptest/rev102srclab`
+ `/child`): `delsnaps -G` deleted the parent's `automated_hourly_a/b`, kept `_c`,
and left `manual_keepme` (pattern) and `child@automated_hourly_child1`
(non-recursive) untouched. Suite: `test/localbackup` includes the non-recursive
assertion + the child-survives negative control.

## Status of the whole finding

| step | scope | where | state |
|---|---|---|---|
| 1-2 | local PUSH source `[prune:<root>]`, distinct src family | **main** | IMPLEMENTED + live-proven |
| 3 | remote PULL source `[prune:<account@host:ds>]` + grant gate | **main** (`d8febbd`) | IMPLEMENTED + live-proven |
| 5 | migration/audit for existing CONFIGs (F3/F4/F5 + F3 residual) | **main** (`d747b35`) | IMPLEMENTED (managed-family effective-retention audit + narrow previewed `--apply`) |
| 6 | continuity after the ordinary common base is lost | **main** (evidence only) | IMPLEMENTED — real-ZFS campaign 2026-08-12, no code change required (see the top section) |

Steps 1-3 and step 5 are now on main. Step 5 adds `audit-source-retention`: the
smallest explicit migration the finding required — a READ-ONLY audit that names
every active pull relationship whose source lacks a bounded source `[prune:]` and
prints the exact sections it *would* add (config byte-identical), plus an opt-in
`--apply` that routes the addition through the existing previewed
`migrate-profile`-style transaction (preserve installed policy, add only what is
missing, per-client fail-closed grant gate, gen-cron validate, preview, confirm,
atomic install). No silent repair, no profile reapplication, no new representation.
This resubmits the full implementation for verification; I do not claim closure.

## Answers to the 7 questions

Full detail in `docs/discussions/PHASE5-SOURCE-RETENTION-DESIGN-2026-08-11.md`
(Q1-Q7 there); summarized here against the current implementation.

1. **Exact native CONFIG sections.** PUSH: `[dataset:<root>]` (send) +
   `[prune:<root>]` (SOURCE, non-recursive, `__src_keep_*`) + `[prune:<target>]`
   recursive (TARGET). PULL: `[dataset:<local-target>] src=acct@host:source` +
   `[prune:<acct@host:source>] ssh_flags=…` (SOURCE remote, `__src_keep_*`,
   non-recursive, monitor-free) + `[prune:<local-target>]` recursive (TARGET). Two
   independent `[prune:]` families from the same preset ladder.
2. **Source-prune recursion/scope.** Follows the dataset's own coverage: the
   default non-recursive source prunes only the named dataset (`delsnaps -G` without
   `-R`); a flat/atomic source would need a matching recursive source prune — not
   exposed yet, so no blanket `recursive=yes`.
3. **Remote prune reuses the relationship SSH.** `ssh_flags` = the pull's own
   transport (`-K LOAD_KEYFILE -k LOAD_ALIAS_KH -O HostKeyAlias=LOAD_ALIAS …`) minus
   `-b`; scope `acct@host:source` is the same identity/host-key pinning as the
   transfer. Live-proven: pve1 collector ran the emitted `delsnaps -G` over the
   zfsbackup account's SSH to source pve2, pruning `hdd/backuptest/rev102remote`.
4. **Grant already includes `destroy`, verified fail-closed, no widening.** Yes:
   `do_commit_scope` (`deploy.sh --commit-scope`) already delegates `destroy` per
   source dataset. `assert_source_prune_grant` reads `zfs allow` over the pinned SSH
   and refuses unless the account's own line carries `destroy` — verify, never
   widen. Fail-closed unit-tested (`test/zfsbackup` section 55: with/without
   `destroy`, ssh error, one-of-many). Confirmed read-only that production source
   datasets hold `destroy`. NOT provable live-negative in this estate: the whole
   `hdd/backuptest` tree grants `destroy` by inheritance, so no live no-`destroy`
   location exists.
5. **No usable bookmark/common snapshot, incl. recursive.** The default equal ladder
   reduces but does not by itself guarantee continuity. The source prune touches
   only `automated_hourly_` snapshots, never bookmarks. The one open sub-decision
   (hard-refuse vs explicit FULL-resend warning on recursive relationships) stays
   tied to live evidence and is NOT decided from a shorthand — see Q4 in the design
   doc. This is the piece I most want reviewer input on.
6. **Smallest migration path (step 5) — IMPLEMENTED.** `zfs-backup.sh
   audit-source-retention` scans the installed CONFIG; for each active pull
   relationship it reads the source scope from the installed `[dataset:]` `src` and
   checks whether the config rendered through `gen-cron.sh` bounds it (F3 — effective
   retention, not a header). Default is read-only: it reports the missing count,
   names each missing section, prints the exact "would add" lines, and touches
   nothing (config md5 unchanged — asserted by `test/zfsbackup` section 57).
   `--apply` appends ONLY the missing source prune(s) via `emit_missing_source_prune`
   (F4 — never `emit_client_sections`, so no topology refresh; refuses on a
   client/CONFIG endpoint disagreement), runs the same per-client fail-closed grant
   gate as activation, validates through the real `gen-cron.sh`, previews via
   `show_activation_proposal`, confirms, then `atomic_replace_and_install` (F5 —
   transaction tested end-to-end). No profile reapplication, no new retention language.
7. **Candidate edit/revalidate/re-preview.** The candidate is CONFIG v4 text; both
   source and target `[prune:]` are separately editable native sections rendered
   through the real `gen-cron.sh`, previewed before `atomic_replace_and_install`
   reads it back — one representation, no second parser.

## Evidence

- main: local-PUSH source retention + F2 non-recursive fix + distinct src family;
  live destructive proof (design doc). `test/localbackup` 42/42.
- main (`d8febbd`): remote-PULL emission + grant gate + REV-106-agnostic split +
  REV-107 preserve-on-reactivation; live remote proof pve1→pve2.
- main (`8cb28f3`): step 5 F3/F4/F5 — narrow `--apply` (F4/F5 ACCEPTED by reviewer).
- main (`d747b35`): F3 residual — managed-family discrimination (exclude bookmark
  `-B`, pattern must cover the managed prefix, finite retention).
  `test/zfsbackup` 386/386 (section 57, 8 assertions: F3 missing / header-no-render /
  bookmark-only / unrelated-prefix / managed-prefix bounded / F5 refusal /
  F4 mismatch-refuse / F4 success topology byte-identical).
- Suites this submission: `test/zfsbackup` 386/386, `test/localbackup` 42/42.
- `./test/impact.sh --verify` clean on main.

## Remaining (my moves)

None. The continuity evidence the earlier revision of this response left open is
supplied above, measured on real ZFS through the delegated account on both engines.

## Requested reviewer input — now answered by measurement, not by prose

The recursive refuse-vs-warn sub-decision (Q5) is settled by the runtime campaign:
the recursive no-base case does not silently degrade and does not need a new guard,
because the engine already refuses explicitly with a non-zero exit and leaves TARGET
history untouched. Q5 in the design doc and item 5 of the "Answers to the 7
questions" section above are superseded by the "Sole remaining blocker" section at
the top of this file.

Two adjacent questions are flagged there for the reviewer's judgement rather than
implemented unilaterally: verifying the `bookmark` grant at deploy time the way
`destroy` is verified, and preferring `-R` over `-r` for recursive relationships so
the bookmark insurance exists at all. Both are outside REV-102's accepted scope.
