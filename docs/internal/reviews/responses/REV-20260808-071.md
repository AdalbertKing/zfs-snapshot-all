<!-- rev: REV-20260808-071 -->
<!-- response-status: IMPLEMENTED -->
<!-- implementation: 96fbfbfa67fb638a16e04640109d660400e9beef -->

# Response to REV-20260808-071

## F1 — IMPLEMENTED

Received trees are derived from the CONFIG topology and classified separately.
Push with a local `dst` gives `<dst>/<source path>`; a **valid** pull section's
own path already ends with the literal remote dataset name, so the receive root
is the section itself. Push with a remote `dst` writes on the peer and is
**not** derivable here — reported as a limit rather than guessed.

Container levels created by a receive are matched **exactly**, not as a subtree,
so `tank/store-other` and `tank/store/something-else` are still found.

Your direction 3 was right and the correction matters more than the code: my
first pull fixture used `[dataset:tank/incoming]` with `src=root@far:far/data`,
a config normal generation **rejects**. It passed only because `--reconcile`
returns before `emit_send` validates the suffix. A test whose config cannot
exist proves nothing, and it had already "passed" twice.

## F2 — IMPLEMENTED

The system anchor now covers its descendants, so `rpool/ROOT/pve-1` is
system-classified. The boundary that was already right survives:
`rpool/data/swap` is deeper than an anchor but not **beneath** one, so it stays
an ordinary finding, as do `rpool/swap-backups` and `rootfs`.

## F3 — IMPLEMENTED, after correcting my own false finding

There is **no config drift**. I published the opposite and was wrong.

I searched `/opt`, `/root/scripts`, the checkout and
`/home/zfsbackup/cron-configs`, found nothing, and reported "the configs do not
live on the hosts" — **without looking at the location `PROJECT_STATUS.md`
names**. Having concluded that, I compared workstation copies and read their
staleness as fleet drift. Your point 2 in the earlier note is what sent me to
check.

Proof per your follow-up point 2 — identity of everything compared, not an
`N/N` summary:

| host | config (`sha256`) | set `sha256` generated = installed | diff |
|---|---|---|---|
| metropolis pve1 | `5c30a24d81a576fe` | `bfbd69cfa236fcb1` = `bfbd69cfa236fcb1` | rc=0, 0 bytes |
| metropolis pve2 | `8c4d23547b9a8fe1` | `03979d81a54ba6a8` = `03979d81a54ba6a8` | rc=0, 0 bytes |
| 11.x pve0 | `f795781418cc9b53` | `72e534b60fa3544c` = `72e534b60fa3544c` | rc=0, 0 bytes |
| 11.x pve1 | `4ddc96600fc6ea83` | `7bcf9b7e2daf7e74` = `7bcf9b7e2daf7e74` | rc=0, 0 bytes |

Run as `zfsbackup` on each host against `/etc/zfs-snapshot-all/<config>`. The
normalisation drops only the logging tail, which encodes **who ran the
generator**, not what the job does.

Nothing was reconstructed. No `cron2conf.sh` work was done.

## Direction 4 — IMPLEMENTED

Coverage now derives from the send entities the generator actually built, and
only from **push** ones. A prune-only section is no longer counted as backed up,
and a pull destination is no longer counted as source-side coverage — that path
is where a copy **lands**.

## F4 — IMPLEMENTED

`docs/testing/RECONCILE-TRUTHSET-2026-08-08.md`. Exact counts, no estimates.

**False negatives: none.** All 19 guest volumes fleet-wide, mapped from
`qm`/`pct config` through `/etc/pve/storage.cfg`, exist and are covered —
including `vm-104-disk-1` and VM 107's efidisk and tpmstate.

**False positives:** of 72 uncovered lines, **11 genuine**, 53 scratch/test, 8
container parents. About **15% signal**, up from ~8%. Received classification
went 0 → 18, which is F1 working on real topology.

Still not good enough to alert on, and I am not proposing it as an alert.

## Evidence

- commit: `96fbfbfa67fb638a16e04640109d660400e9beef`
- `./test/reconcile/run.sh` **29/29**; graph cascade for a `gen-cron` change:
  `test/run.sh` 65/65, `migrate` 52/52, `profiles` 22/22, `zfsbackup` 292/292,
  `impact` 56/56
- negative control vs `8b578bd`: exactly the four new assertions fail, all 25
  pre-existing pass
- mutation control: unanchoring the system match fails the nested-`swap` boundary
- four-host run against proven-authoritative host-local configs
- no Stage-2 rerun, per your boundary

**One case does not discriminate and is labelled in the suite rather than
counted:** removing the remote-`dst` guard leaves it passing, because a root
built from `root@peer:…` never matches a local dataset name anyway.

## Two decisions I am not taking

1. **Container parents.** Suppressing "a dataset with no data of its own whose
   children are all covered" is mechanical and looks safe, but "holds no data"
   needs defining (`used` minus `usedbychildren`? `written`?) and a wrong
   definition hides a real dataset.
2. **Scratch.** The explicit owner-managed ignore list you sketched, after the
   detector is correct.

Both belong to you and the owner. Until they are settled, 15% signal is what the
tool honestly produces, and an alert on that would be ignored within a week.
