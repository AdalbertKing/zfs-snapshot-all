<!-- rev: REV-20260902-133 -->
<!-- response-status: IMPLEMENTED -->
<!-- implementation: 6534cb13124b45b5f0508c9335d16ece43a104f5 -->

# Response to REV-20260902-133

## F1 — IMPLEMENTED

### Analysis

Correct, and the finding outlived its own fix. The unescaped `Doba:` span
from PR #260 and the second span with parentheses from PR #263 were both
escaped in the digest deliveries that followed (`be067a3` and earlier:
`deploy.sh` line 5886 now reads `\`Doba:`), and the suite gained a static
grep over every comment line of the extracted body. So on current `main` the
generation is already inert: rendering the heredoc prints nothing.

What was still missing is the half the review actually asked for. The static
grep reads the text; it cannot say what bash DID while expanding it. The suite
had reported 37 PASS / 0 FAIL on `d3cd8d3` and 46 PASS / 0 FAIL on
`9aae4f3` while the render was executing a command and, later, failing to
parse -- because the render's stderr went to the terminal and no assertion
looked at it. A property that only a person reading the log can notice is a
property the suite does not have.

### Planned or implemented change

`test/alertmail/run.sh`, at the render site: the expansion now runs with a
sentinel directory first on `PATH` holding an executable literally named
`Doba:` that records being called, and with stderr captured. The assertion is
that stderr is empty AND the sentinel was never called. No code change to
`deploy.sh` is needed on `main`; the acceptance criteria 1 and 2 are met by
the already-published escapes and the v10 behaviour they preserve, and this
delivery keeps the scope to criterion 3 (criterion 4: no staleness semantics
added).

### Evidence

- commit: `6534cb13124b45b5f0508c9335d16ece43a104f5` (test only), on branch `claude/package-translation-estimate-jisaqu`, PR #295. A separate `fix/REV-20260902-133-*` branch was refused at the permission prompt, so the correction rides the branch the Owner designated for this session.
- commands run:
  - `./test/alertmail/run.sh 2>am.err` on the branch: **79 PASS / 0 FAIL, stderr 0 lines**.
  - `DEPLOY_SRC=<deploy.sh @ d3cd8d3> ./test/alertmail/run.sh`: the new assertion **FAILS** -- `sentinel called: YES`.
  - `DEPLOY_SRC=<deploy.sh @ 9aae4f3> ./test/alertmail/run.sh`: the new assertion **FAILS** -- `sentinel called: YES`, stderr carries `syntax error near unexpected token \`('` (the second span).
  - `./test/impact.sh --verify`: clean (below, after the ledger regeneration).
  - exact-head CI: the PR's run on the head carrying this commit.
- results: as listed; the control discriminates both reviewed heads from the fix.

### Remaining risk

The sentinel proves that THIS comment's command is not run; the empty-stderr
half is what generalises to any future span, because an unquoted heredoc that
expands prose either runs something (usually loudly, `command not found`) or
fails to parse it. A span that happens to name an existing command with no
output would still slip past the stderr half and be caught only by the static
grep above it -- the two checks are kept together for that reason.

## Follow-up 2026-09-03 — the mechanism behind F1 is gone (commit `345ce58`)

On the Owner's direction, the four scripts a host runs (`notify-fail.sh`,
`notify-warn.sh`, `alert-digest.sh`, `check-pool-capacity.sh`) stopped being
unquoted heredocs in `deploy.sh` and became files in `hostscripts/`, installed
by copying (`host_script_ensure`: byte-identical to the checkout means
current). Nothing is expanded at install time any more, so the property F1
asked for -- rendering executes no comment text and prints nothing -- holds
by construction rather than by guard.

The two acceptance controls this response added to `test/alertmail`
(comment-expansion grep, `Doba:` sentinel during a live render) were removed
with the mechanism they measured; what stands in their place is the count of
heredoc openers for the four scripts (0; `main` before the change: 4) and the
`host-scripts` section that runs the real installer against a throwaway
target. The acceptance boundary of this REV therefore changed shape after the
submitted SHA `6534cb1`; the `implementation` fact above is left as submitted
and closure remains the Reviewer's call. The live proof of the new shape is
`docs/discussions/LAB-HOSTSCRIPTS-2026-09-03.md`, not yet executed.
