<!-- rev: REV-20260809-082 -->
<!-- response-status: IMPLEMENTED -->
<!-- implementation: 3562bfb0fa0b02c67efc53b2527495acf8843a1a -->

# Response to REV-20260809-082

All three findings ACCEPTED.

## Header correction (2026-08-09, per owner's LIVE-PROOF-CONSOLIDATION note)

This response's `implementation:` header pointed at `a1bfa19f...`, the commit
the reviewer's V1/V2 follow-up (below the original F1/F2/F3 sections) found
still incomplete: the F1 field-survival test stopped at the emitted stanza
TEXT instead of the real `gen-cron.sh` render, and the F2 impact regression
asserted the `deps.conf` declaration rather than the resolved plan through a
real interface that didn't exist yet (`--for`/`--suites-for`).

Both were corrected in `3562bfb0fa0b02c67efc53b2527495acf8843a1a` ("REV-082
V1+V2: the fallback I wrote to be robust made the test lie"), already on
`main` — this header now points at it. I independently re-verified both
fixes are actually present in the current tree rather than trusting the
commit title:

- `test/zfsbackup/run.sh` composes a complete candidate (defaults + rendered
  templates + emitted stanzas), runs it through the real `gen-cron.sh`, and
  asserts the profile's `recursive = flat` reaches the rendered transfer line
  as `-R` — not just the emitted section text.
- `test/impact/run.sh` calls the real `"$IMPACT" -f lib-profile.sh` interface
  and asserts the resolved plan, not an `awk` read of the declaration.
- `bash test/zfsbackup/run.sh` — **310 passed, 0 failed** (301 immediately
  after `3562bfb` per that commit's own message, plus 9 unrelated
  REV-083/084 overlap-guard cases added afterward in `57deecb`)
- `bash test/impact/run.sh` — **58 passed, 0 failed**

F1/F2/F3 below are otherwise unchanged from the original response; only the
implementation pointer and this section are new.

## F1 — ACCEPTED (IMPLEMENTED)

The finding is right and it names the shape correctly: a generic validated
fragment followed by a hand-maintained key-to-key extractor is more machinery,
not less. `profile_emit()` now copies the rendered fragment through. The
relationship still writes what it owns — prune `recursive`, `pair_label`,
`notify`, `src` — and cannot collide, because `lib-profile.sh` refuses exactly
those fields inside a profile.

The owner'''s preinstall-preset direction and this finding agree, which is why I
implemented it without waiting: both say the high-level layer should stop
translating a contract `gen-cron.sh` already owns.

### What writing the test found, which is worse than the finding

Mid-edit, `profile_emit` was referenced by all three call sites and **not yet
defined**. Client sections were losing their entire policy reference.

**The suite reported 295/295.**

Nothing in it asserted that a generated `[dataset:]` carries a `use_template`
line at all. So the false green F1 describes was not limited to unknown fields —
the *known* field was unasserted too, and a runtime that emitted policy-free
sections would have shipped green.

I caught it only because the number did not move where it had to move. The new
block therefore pins both: the field that was always there, and the field the
extractor dropped.

## F2 — ACCEPTED (IMPLEMENTED)

`lib-profile.sh` declares `suites = profiles zfsbackup`. B1 made
`zfs-backup.sh` source it, so a profile-boundary change can break the runtime,
and the graph was routing such a change only to `profiles`.

The regression asserts the **real** graph rather than a fixture, deliberately: a
fixture would prove the resolver can follow an edge, which was never in doubt.
Whether *this* edge is declared is what was, and only the real file can answer.

## F3 — ACCEPTED (IMPLEMENTED)

The stale sentence was mine, written one commit before it stopped being true.
Replaced with a single statement, not repeated lower down: the runtime reads a
profile, but nobody selects one — one preset, new enrolments only, and CONFIG v4
is the execution truth.

## Evidence

- commit: `a1bfa19f814bdb05d8ca22f2821b5ca62610ec80`
- `./test/zfsbackup/run.sh` — **299/299** (was 292)
- `./test/impact/run.sh` — **58/58** (was 56)
- negative control against `0dccb0be`, copy at the repo ROOT so `SCRIPT_DIR`
  resolves its libraries: **exactly one** assertion fails, the F1 field-survival
  case. The other three pass there too and are regression cover, not proof.
- `./test/impact.sh --verify` clean, exit status read directly, not through a
  pipeline

## Disclosure about the test runs

Three earlier `impact` runs are **not** reported as evidence: I stashed
`deps.conf` while one was running and edited the suite file while bash was still
reading another. Only the final run on a frozen tree is quoted above.

## Remaining risk

`profile_emit` copies whatever the boundary accepted. That is the intent, and it
moves the question entirely onto `lib-profile.sh`'''s field rules — which is where
the contract says it belongs, but it does mean a mistake in the forbidden-field
list now reaches a config directly rather than being filtered by a narrow
extractor. I consider that correct rather than risky, and state it so the trade
is on the record.

## Status of the reviewer's V1/V2/V3 follow-up

- **V1** (field-survival test must go through the real `gen-cron.sh`, not
  just assert the emitted text): FIXED in `3562bfb`, verified present above.
- **V2** (impact regression must assert the resolved plan through a real
  interface, not the `deps.conf` declaration text): FIXED in `3562bfb`,
  verified present above.
- **V3** (bounded live-host apply/preview/read-back/refusal proof on the real
  high-level path): still open. Per the owner's
  `docs/discussions/LIVE-PROOF-CONSOLIDATION-2026-08-09.md`, this should be
  satisfied by ONE bounded live campaign shared with REV-083's own live
  overlap-refusal proof, using a single throwaway relationship, rather than
  two separate live campaigns. That campaign has not been run — see the
  REV-083 response for why it could not be started unattended (no currently
  active controlled relationship exists on any reachable host, and creating
  one requires a real `zfs send`/`receive`, explicitly out of scope without
  an approved live-test window).
