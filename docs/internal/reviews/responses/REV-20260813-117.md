<!-- rev: REV-20260813-117 -->
<!-- response-status: IMPLEMENTED -->
<!-- implementation: 76419289d442c13364be272d112749be6a78e83c -->

# Response to REV-20260813-117

Bookkeeping finding, accepted in full. No product behaviour was touched; the Phase 7
planner accepted under REV-116 is unchanged.

## F1 — IMPLEMENTED — the status document was defective by its own rule

`docs/PROJECT_STATUS.md` states that a refresh date older than the last
behaviour-changing commit is a defect to report rather than a detail. It was
exactly that: `2026-08-11` against `306cdba` on `2026-08-12`.

The date now names the commit it is measured against, and the human-facing block
says what Phase 7 actually is on `main` rather than implying completeness:

- slice 1, the read-only planner;
- slice 2, safe restore into the derived namespace via staging and `zfs rename`
  promotion;
- the full planner map (guid + CONFIG-sourced consistency);
- **and explicitly: destructive replace does not exist**;
- plus the fact that the owner changed the default recovery policy on 2026-08-12,
  so the planner/CLI are due a redesign under
  `OWNER-RECOVERY-DEFAULT-POLICY-2026-08-12.md`.

That last point matters more than the date. A reader who saw only "Phase 7 slices
1 and 2 delivered" would reasonably assume the current planner implements the
current policy. It does not yet.

The machine marker was refreshed with `./test/impact.sh --refresh-status`, not by
hand.

## F2 — IMPLEMENTED — routing regenerated from the corrected headers

Regenerated, not hand-edited. Thank you for correcting REV-116's
`reviewed-implementation` on your side; I could not have fixed that file, and
generating over the mismatch would have persisted an INVALID state rather than
exposing it.

Worth recording why the mismatch existed, because the mechanism is not obvious: an
evidence-only resubmission cannot move the ledger, since routing keys on
`reviewed-implementation != implementation`. I therefore named the commit carrying
the transcript as the implementation — which was right, and which put my header
one SHA ahead of your approval until you advanced it. The tooling caught the
disagreement instead of averaging it, which is the behaviour we want.

## Required proof

Captured on pve1 at published `726930c`:

**3. `reviewctl --generate` then `--verify`**

```
reviewctl: wrote docs/internal/reviews/REVIEW_LEDGER.md and docs/project/OPEN-THREADS.md (53 reviews)
reviewctl: ledger and routing agree with the artifacts (53 reviews)
```

**2. status marker + impact verification**

```
  reviewctl: ledger and routing agree with the artifacts (53 reviews)
graph is consistent with the tree
```

**4. REV-116 CLOSED, and no INVALID review remains**

```
| REV-20260812-116 | CLOSED | - | 6ebb1d45257dd05994f1140d3f644525aac7bd5b | 6ebb1d45... |
INVALID rows in the ledger: 0
```

**5. resulting OPEN-THREADS**

```
| REV | State | Whose move | Next action |
| REV-20260813-117 | OPEN | Claude | implement and respond |
```

REV-117 correctly shows as mine while this response is being written; it moves to
the reviewer when this response names its implementation SHA.

No ZFS, SSH, crontab or deploy proof was run. None was required.

## Remaining risk

None for the finding itself. One standing observation: PROJECT_STATUS drifted
because its human-facing date is maintained by hand while the machine marker is
enforced by tooling — so the marker can be green while the prose is stale, which is
precisely what happened. I am not proposing to automate the date; the prose has to
be written by whoever knows what changed. But the failure mode is real and this is
the second time today that a hand-maintained line disagreed with a generated one.

Not claiming closure.
