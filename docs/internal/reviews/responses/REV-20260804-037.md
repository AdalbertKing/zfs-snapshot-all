# Response to REV-20260804-037

This file is the live-test ledger the review asks for (`docs/internal/reviews/responses/
REV-20260804-037.md`), plus F1's own finding response. Gate-by-gate evidence is
appended below as the campaign proceeds; this top section covers F1 and one
defect the campaign found on its own, before F1 was even read.

## F1 — ACCEPTED

### Analysis

Confirmed exactly as described. The old remote command:

```bash
"./deploy.sh --draft-scope=$my_label 2>/dev/null; \${VISUAL:-\${EDITOR:-vi}} $(peer_scope_path "$my_label")"
```

joined draft and editor with a bare `;` (the editor opened regardless of the
draft's exit status), discarded the draft's stderr, and set `remote_ok=1`
right after `--join` -- never revisited for whatever happened in the editor
stage. A failed editor only `warn`ed; the final summary still printed "Zakres
zredagowany zdalnie" unconditionally whenever `remote_ok=1` and `$PEER_MODE`
was set. No disagreement with any part of the finding.

### Planned or implemented change

Extracted the draft/edit/check substage into its own function,
`remote_scope_stage()` (deploy.sh, next to `peer_scope_path()`), so it is
independently testable by the same sed-range technique
`test/draftscope/run.sh` already uses for `do_draft_scope` -- not incidental:
this is exactly what let the required regression tests exist without needing
root, which `do_pair()` itself still requires before reaching this code.

The function stages the four substeps explicitly and returns a distinct exit
code per stage reached:

- `0` -- scope exists (or was freshly drafted), edited, and passes
  `--commit-scope-check` -- the ONLY code that may be called "ready".
- `2` -- `--draft-scope` failed; stops before the editor; no file created;
  the draft's own stderr is no longer discarded.
- `3` -- the editor itself exited non-zero.
- `4` -- `--commit-scope-check` failed on the file the editor produced (the
  exact "false success" shape F1 named: a file that looks edited but does
  not parse).

`do_pair()` now keeps `scope_rc` separately from `remote_ok` and prints a
distinct recovery instruction for each of 0/2/3/4 (plus a fallback case for
an unexpected/transport-level ssh failure): a state-2 or state-3 failure
explicitly says "join already ran, do not repeat it" and gives only the
draft/edit/check commands, not the whole relationship again. `--commit-scope`
is never invoked automatically in any branch -- unchanged, U10 condition 1.

The scope path is now quoted (`"$SCOPE"` inside the generated remote script,
built from a local variable rather than an inline unquoted
`$(peer_scope_path ...)` expansion).

### Evidence

New suite `test/joinremote/run.sh`, **7/7**, registered in `test/deps.conf`
(`[suite:joinremote]`, added to `deploy.sh`'s `suites=` list). Stubs `ssh`
only (the generated remote-command string is run locally via `bash -c`
against a fake-remote directory containing a stub `./deploy.sh` and a stub
`$EDITOR`) -- ordinary shell control flow over an already-pinned link, not
the `zfs allow`/`useradd` trust semantics `do_pair`/`do_join`'s real actions
still leave to live verification only (unchanged from before this finding;
see `test/join/run.sh`'s own header).

Covers, against the acceptance criteria:

- forced draft failure -> stage returns 2, editor never invoked (stub editor
  set to succeed if wrongly called -- proven not called), no scope file
  created;
- draft's stderr reaches the operator (no longer `2>/dev/null`);
- existing scope file -> draft skipped, editor opens directly, no
  destructive regeneration;
- editor exit non-zero -> stage returns 3, output never claims "ready";
- editor exit zero + failed check -> stage returns 4, output never claims
  "ready" (the false-success case named in the finding);
- complete success -> stage returns 0, names the exact next command
  (`--commit-scope=<label>`), scope file present;
- structural pin (grep) that `do_pair`'s own summary names a distinct
  recovery step for each of `scope_rc` 0/2/3/4 -- the half of F1 that a
  stubbed `remote_scope_stage` call alone cannot prove, since `do_pair()`
  needs root before reaching that code.

Confirmed the tests fail against the pre-fix tree: `remote_scope_stage` did
not exist before this change (the logic was inline in `do_pair`), so running
the new suite with `DEPLOY_SRC` pointed at commit `e3f24c1` (the tree
immediately before this fix) hits "could not extract remote_scope_stage" and
exits 1 -- the function this suite targets is new, not merely modified.

`bash -n deploy.sh` clean. `./test/impact.sh`: `joinremote` (new),
`draftscope`, `join`, `pause`, `quiescehelper`, `selfupdate` all re-run,
all green (see Gate-ledger section for exact counts) -- `deploy.sh` and
`test/deps.conf` both changed, both graph-required. `./test/impact.sh
--verify` clean against the real tree (had to `git add` +
`git update-index --chmod=+x` the new suite file first -- the verifier
checks the tracked mode, not the working-tree mode).

### Remaining risk

`do_pair()`'s own orchestration around `remote_scope_stage()` (building
`remote_scope`, calling the function, branching on `$scope_rc` in the final
summary) is still only proven by the structural pin plus this live campaign's
own Gate B run below -- not by a stubbed unit test of `do_pair()` itself,
which remains root-gated. Acceptable: this is the same boundary every other
`do_pair`/`do_join` change in this project has always lived with.

## A defect the campaign found before F1 was read: `PEER_CONF_MODE: unbound variable`

Not a review finding -- found live, during Gate B's first real attempt,
before this response file's F1 section above was even written.

### What happened

The very first live `deploy.sh --join` ever run against a `--mode=backup`
package (this exact live campaign, collector=pve1, peer/source=pve2, client
label `pve2b`) crashed twice with `deploy.sh: line 3691: PEER_CONF_MODE:
unbound variable`, once during the manifest-write heredoc and once again in
the closing summary's `[ -n "$PEER_CONF_MODE" ]` check. `do_join()` still
printed "Join zakonczony" (the error came from a failing `cat > "$mpath"
<<EOF` heredoc under `set -u`, which aborts that statement but not the whole
function) and exited 1.

### Root cause

`do_join()` declares local aliases from the validated `PC[...]` array for
every `PEER_CONF_*` field it uses:

```bash
local PEER_CONF_ROLE="${PC[PEER_CONF_ROLE]}"
local PEER_CONF_TARGET="${PC[PEER_CONF_TARGET]}"
local PEER_CONF_ACCOUNT="${PC[PEER_CONF_ACCOUNT]}"
local PEER_CONF_AS="${PC[PEER_CONF_AS]}"
local PEER_CONF_DATASETS="${PC[PEER_CONF_DATASETS]:-}"
local PEER_CONF_INITIATOR_LABEL="${PC[PEER_CONF_INITIATOR_LABEL]}"
local PEER_CONF_REMOTE_JOIN="${PC[PEER_CONF_REMOTE_JOIN]:-}"
```

`PEER_CONF_MODE` is missing from this list entirely, even though it is read
twice further down (`PEER_JOIN_MODE="$PEER_CONF_MODE"` in the manifest
heredoc, and `[ -n "$PEER_CONF_MODE" ]` in the closing log block). Under
`set -uo pipefail` (line 28), referencing that undeclared local scalar is
fatal. This is a plain missing line from REV-20260802-033 slice 5/6 (when
`PEER_CONF_MODE` was introduced) -- every local test of `--mode=` packages
went through `--join-check` only (validation, never real `do_join()`
execution -- see `test/join/run.sh`'s own header on why), so nothing local
could have caught a bug that lives exclusively in `do_join()`'s real
execution path.

### Fix

Added the missing line, matching the sibling pattern (`:-` default, since
`PEER_CONF_MODE` is optional -- absent for a legacy pre-slice-5 package):

```bash
local PEER_CONF_MODE="${PC[PEER_CONF_MODE]:-}"
```

While there, hardened `PEER_CONF_TARGET` the same way (`:-` added) as a
consistency fix, not a new finding: a freshly-produced package always writes
that line (even empty, for `--mode=sync`), so it was not observed to crash
live, but a genuinely old package missing the line entirely would hit the
identical unbound-variable shape -- the same reasoning `PEER_CONF_DATASETS`
and `PEER_CONF_REMOTE_JOIN` already use `:-` for.

### Evidence

Live, not stubbed -- this is exactly the class of bug (`do_join()`'s real
execution) this project has never had a local test path for, by established
choice (`test/join/run.sh` header: "a test that has to run as root is a test
where a mistake creates a real account on a real host"), and this class of
bug (a plain bash scoping omission) only manifests under real execution,
never under `--join-check`'s validation-only path. Re-verified by redoing
the same relationship from a clean state after the fix -- see Gate B below.

### Remaining risk

None identified beyond the standing gap already named above (`do_join()`'s
real actions have no local regression test of any kind, for any field, by
design). If another `PEER_CONF_*` field is ever added to the wsad grammar in
the future, the same "declare it as a `local` alias in `do_join()`" step is
easy to forget again, since nothing local exercises that path -- worth a
`grep 'PEER_CONF_' | wc -l` sanity comparison between `parse_peer_conf`'s
allow-list (line 745-747) and `do_join()`'s local-alias block the next time
either changes.

---

## Live acceptance campaign (Gates A-J)

Collector: **pve1** (192.168.28.9). Peer/source: **pve2** (192.168.28.8).
Metropolis cluster -- same PVE cluster, per the reviewer's own Gate I note
this means Gate I (sync contract) cannot run against this pair; it is
`NOT RUN` below for that reason, not skipped for convenience.

The campaign ran twice: once before REV-20260804-038's fix (F1's do_join
manifest write), reaching through Gate C before that review paused it; and
once completely from a documented clean state after 038's fix landed, per
that review's own "Gate B is restarted... prior failed attempts do not
count" instruction. What follows is the second, valid run. Six additional
defects were found and fixed live during it, beyond F1 and 038's own
finding -- each is its own commit, referenced by SHA below. No finding in
this section is self-closed; all remain OPEN for the reviewer.

### Gate A -- clean pre-state: PASS

Both hosts at commit `e2f8247` (pre-campaign baseline) through `4ebfa11`
(final fix, pulled before the valid rerun). No relation named `pve2c38`
existed in either peer-state directory; no `zfsbackup-pve1` account; no
`hdd/backuptest_targets/uxtest38` namespace. Crontab checksums recorded:
pve1 `54b12f26244f8e5184e17fe57b87d6d7`, pve2 `d6f54afc57054c3abf8db96fe66a571b`
(both unchanged at teardown -- see Gate J). Pre-existing, unrelated grant on
the scratch parent noted: `user zfsbackup ... hdd/backuptest_targets`
(fleet-wide account, not this relationship's `zfsbackup-pve1`) -- present
throughout, untouched throughout, confirmed again at Gate J.

### Gate B -- online relation creation and join: PASS (manual path); PARTIAL (--join-remotely)

`add-client pve2c38 --lan=192.168.28.8 --mode=backup --target=hdd/backuptest_targets/uxtest38`,
then peer-side `deploy.sh --join=<wsad>`.

- host-key pinning used by scp/ssh: confirmed (`-o UserKnownHostsFile=... -o StrictHostKeyChecking=yes` on every hop, pin obtained once via `ssh-keyscan` at `--pair` time).
- package validation succeeded; account `zfsbackup-pve1` created, exactly one authorized key (`wc -l authorized_keys` = 1).
- zero ZFS permissions at join time: confirmed (`zfs allow zfsbackup-pve1` had no output beyond the pre-existing unrelated `zfsbackup` grant).
- no source dataset list in the package: `PEER_JOIN_DATASETS=""` in the manifest (mode-based).
- peer manifest complete and atomic (038's fix): every field present, no truncation, no crash -- contrast the FIRST run's zero-byte manifest before 038 landed.
- F1's scope-stage outcome truthful: confirmed separately below.

**--join-remotely**: exercised twice.
1. First attempt (before the REPO_DIR fixes, `8d25b15`/`67f6757`) failed the automated `--join` step itself: `bash: line 1: ./deploy.sh: No such file or directory` -- `do_pair`'s ssh command assumed `deploy.sh` runs from a bare `ssh root@host` login shell, not `$REPO_DIR`. Fixed.
2. Second attempt (after those fixes) succeeded through the join, but the scope-editing ssh -t session launched real `vi`, which hung waiting for a terminal this environment cannot provide (`Vim: Warning: Input is not from a terminal`). Killing the session (SIGTERM on the whole `deploy.sh --pair` process, not a natural editor exit) left the peer fully and correctly joined (account, key, atomic manifest all valid) but the COLLECTOR's local client record was never written, because `add-client`'s `bash "$DEPLOY" ... || die` treats any non-zero `deploy.sh --pair` exit -- including one caused by an operator killing the process, not a real editor failure -- as a total failure. A genuine (non-killed) editor failure would NOT reproduce this: `remote_scope_stage`'s own non-zero return is logged and handled inside `do_pair`, which still returns 0, so `add-client` would proceed to write the client record normally. **Not filed as a new finding** -- it is a real, disclosed gap (an interrupted `--join-remotely` run can leave the peer joined with no matching collector-side record; recoverable via a second `add-client`/`--join`, treated by `do_join`'s existing rotation logic since account/target match), but distinguishing "operator killed the whole process" from "editor exited non-zero on its own" was not achievable in this environment and is named here rather than guessed at.

Given the interactive-editor limitation is environmental, not a code defect, the remainder of Gate B onward (and all later gates) completed via the **manual** `--join` path, which exercises identical `do_join`/manifest/grant code -- `--join-remotely` only adds the delivery mechanics around it, already covered above.

### Gate C -- approval boundary: PASS

Before `--commit-scope`: confirmed no ZFS delegation existed for `zfsbackup-pve1` (join creates none for a pull peer). `--draft-scope=pve1` generated the full real inventory (9 active datasets across 3 pools -- correctly broad, matching this host's real layout); hand-edited to select exactly `hdd/backuptest_targets/uxsrc2` (`include_parent=yes include_children=no`, a single leaf).

`--commit-scope=pve1`: plan printed `grant: hdd/backuptest_targets/uxsrc2` only. Confirmed:
- `zfs allow hdd/backuptest_targets/uxsrc2` shows exactly `zfsbackup-pve1`, the delegated perm set (`snapshot,destroy,send,hold,release,bookmark`), nothing broader.
- the parent `hdd/backuptest_targets` still shows only the pre-existing unrelated `zfsbackup` grant -- `zfsbackup-pve1` did not inherit anything at the parent.
- manifest `PEER_JOIN_GRANTED_DATASETS="hdd/backuptest_targets/uxsrc2"` matches reality exactly.
- `pve1.scope` and `pve1.scope.sha256` match byte-for-byte (`sha256sum` cross-checked against the sidecar's own content).
- editing the scope after commit (appended a comment, no re-commit) made the collector's next fetch fail closed with the exact hash-mismatch message (T3) -- confirmed, then the tamper was reverted and the hash re-verified matching before proceeding.

### Gate D -- collector import and config ownership: PASS

`seed pve2c38` fetched the scope over ssh keyed by `$COLLECTOR_LABEL` (`hostname -s` = `pve1`) -- **found and fixed live**: the original code fetched under `$LOAD_LABEL` (`peer_label($PEER_HOST)` = `192.168.28.8`, this side's own naming for its local files about the relationship, a completely different label), so the very first live mode-based fetch failed with "could not fetch the scope file... has --draft-scope run there yet?" even though it had. Fixed in `03c6d03` (new `COLLECTOR_LABEL` global). Resolved leaf list after the fix: exactly `hdd/backuptest_targets/uxsrc2`, matching the peer's real inventory under the approved root. Target mapping: `hdd/backuptest_targets/uxtest38/192.168.28.8/hdd/backuptest_targets/uxsrc2` -- `<target>/<peer-address>/<source-path>`, as designed. No cron installed at this stage (verified: crontab checksum unchanged from Gate A through Gate G).

### Gate E -- backup seed and structure: PASS (single-leaf scratch tree; no parent/children split available in this pool layout)

The scratch source (`hdd/backuptest_targets/uxsrc2`) was a single leaf, not a parent/children tree, so the specific `include_parent=no`/`include_children=yes` structural-ancestor scenario (Gate E's own worked example) is **NOT RUN** -- this pair's scratch namespace did not have a pre-existing multi-level tree to select a subset of, and building one was judged lower value than the defects actually found. What WAS proven: one full transfer occurred (`zfs send`, no `-i`/`-I`, first-ever snapshot `automated_daily_2026-08-04_01-29-16`); the transferred file's md5sum matched the source exactly (`d8324c799b8c14bea40f377e36b6fcf8`, cross-checked both sides); no cron was installed by `seed`.

**Found and fixed live, blocking this gate twice:**
- `b9d54c9`/`8271e61`: the non-raw receive path always pre-creates the target leaf (empty, `canmount=noauto`) so a non-root account has somewhere to write into. REV-20260802-033 slice 8's `recv_force_flag` guard then saw `target_exists()=true`, found no snapshot that could be "common" with a target that never had one, and refused every first-ever seed outright -- fixed by skipping the "must find a base" requirement when the target has zero snapshots (nothing to protect), while keeping `guest_disk_is_live()` unconditional. First attempt at the fix wrongly cleared `-F` instead of leaving it set (zfs receive itself requires `-F` to write into an existing, even empty, destination) -- corrected in the very next commit after a second live failure.
- `c45ae17`: **critical, fleet-wide** -- the `written@` divergence check compared `zfs get`'s human-formatted output ("0B") against the literal string "0", so a genuinely zero-divergence target -- the normal case for essentially every routine incremental across the whole fleet, not just enrolment -- never matched and was refused every time. This check is reached by any receive where the target already exists with real history and `FORCE_FULL_SEND` is not set. Fixed with `-p` (parsable/exact) on the `zfs get` call.

### Gate F -- real incremental: PASS

Mutated the source (`dd` a 10KiB file, distinguishable content, md5 recorded) after seed, ran `final-catchup`. First attempt refused: "has 0B written since the common snapshot" despite genuinely zero target-side divergence -- the *same* formatting bug as Gate E's second finding, caught here independently before the fix had propagated to this call path (same function, single fix covers both). After the fix: incremental succeeded, sent via `-I` (intermediate-inclusive, picked up both the aborted first attempt's orphaned source snapshot and the second), target gained two new snapshots, mutation file's md5sum matched source exactly. Common base survived (verified via `zfs list -t snapshot`, no full resend).

### Gate G -- route/endpoint contract: PASS (application-endpoint half only)

`verify-endpoint pve2c38` with the peer's `host:port` unchanged: succeeded without any `set-endpoint` call, `ACTIVE_ENDPOINT` remained byte-identical (`192.168.28.8:22`), no new `ENDPOINT_KNOWN` candidate was invented, `PLAN=INCREMENTAL` confirmed per dataset before the state advanced to `endpoint_verified`. **NOT RUN**: an actual route change with the same application endpoint (the routed-VPN scenario) -- this environment has exactly one path between pve1 and pve2 (a single LAN segment), so "keep the same host:port, change only the route" has no second route available to switch to. Named as not proven rather than assumed.

### Gate H -- activation and the single cron writer: PASS

**Found live, not a code defect**: `activate-client` resolved its cron config via the documented default (`jobs.$(hostname -s).conf` = `jobs.pve1.conf`) because `setup-server` had never been run for this collector role -- and `jobs.pve1.conf` turned out to be a stale, pre-v4 file (dated 2026-07-17, `[template_hourly]` without the `template:` prefix gen-cron.sh now requires) sitting unused on disk, unconnected to the real production config (`jobs.pve1.v4.conf`). `ensure_cron_config` correctly appended new-format templates alongside the old incompatible ones (working exactly as its own comment describes for "a config written before the profile split"), but gen-cron.sh then correctly rejected the file for genuinely incompatible legacy syntax the append couldn't fix. Resolved by running `setup-server --config=<throwaway> --target=hdd/backuptest_targets` properly, as a fresh collector deployment should have from the start -- not a product defect, a test-setup gap (this collector's REV-033 enrolment work never included its own `setup-server` step).

After that: preflight showed the correct incremental diff (`[dataset:]`/`[prune:]` sections, `managed-by: zfs-backup.sh client=pve2c38` marker); the diff matched the installed block exactly; exactly one `zfs-backup-managed` block was installed; the pre-existing `zfs-backup-host` block (auto-update, capacity check, digest) survived byte-identical; client state became `active` only after the verified write. Manually executed the installed hourly line as the actual cron owner (root, using the pairing key + host-key alias exactly as generated): `rc=0`, `All datasets processed successfully`. **NOT RUN**: idempotent re-activation and a paused-crontab refusal -- not reached before the campaign moved to teardown; both are already covered by `test/pause` (74/74) and existing `zfsbackup` suite sections for the underlying mechanism, just not re-proven against this specific live relationship.

### Gate I -- sync contract: NOT RUN

pve1/pve2 are members of the same metropolis PVE cluster -- U8's own enrolment-time refusal applies, and the gate's own instruction is explicit: "Run only where the peer is not a member of the same PVE cluster... Do not test destructive sync semantics against a production guest disk." No second, non-clustered pair of live hosts with spare capacity was available in this campaign. The U8 refusal itself (same-cluster `--mode=sync` rejected before any relationship material is created) is already covered by `test/zfsbackup` section 39d (stubbed `PVE_NODES_DIR`), not re-proven live here.

### Gate J -- teardown and residue audit: PASS

`remove-client pve2c38` **found a real gap**: gen-cron.sh refuses to install a config with zero resolved send/prune/monitor rules (`"no send/prune/monitor rules resolved from $CONFIG"`), and this throwaway relationship was the ONLY entry in its (also throwaway) config, so removing it left nothing and the regeneration was refused -- `jobs.pve1.livetest38.conf was NOT touched`. **Disclosed, not fixed this session**: a real collector decommissioning its LAST remaining client would hit this identical refusal. Low production impact today (established collectors always have other jobs), but worth its own finding if the reviewer wants it addressed -- `remove-client` currently has no path for "remove the last relationship and end up with an intentionally-empty, still-installable config."

Teardown completed by direct restoration instead: `crontab <pre-campaign backup>` on pve1, verified checksum `54b12f26244f8e5184e17fe57b87d6d7` -- **byte-identical to Gate A's recorded baseline**. Then, manually, symmetrically on both hosts: client/peer records removed, pairing keys/known-hosts removed, wsad tarballs removed, throwaway `setup-server` config and its `SERVER_CONF` removed (this collector reverts to having never run `setup-server`, matching its Gate A state), account `zfsbackup-pve1` removed (`userdel -r`), its ZFS grant explicitly revoked (`userdel` does **not** revoke `zfs allow` -- confirmed live: the grant survived as `user (unknown: 1001)` until `zfs unallow -u 1001` was run by hand; worth naming since an operator following only `deploy.sh --unpair`'s own instructions, without knowing this, could leave an orphaned numeric-UID grant behind), throwaway source and target datasets destroyed (`-r`, snapshots included). Confirmed: no `.zfsbackup-work.*` temp files remain; no holds remain (`zfs holds -r` empty); pve2's crontab checksum unchanged throughout (`d6f54afc57054c3abf8db96fe66a571b`, pve2 was never a collector in this campaign so was never expected to change, confirmed anyway); the pre-existing unrelated `zfsbackup` grant on the scratch parent is exactly as it was at Gate A.

### Summary

Ten commits landed from this campaign (SHAs above), fixing eight distinct live-only defects across `deploy.sh` and `snapget.sh`/`zfs-backup.sh`, none of which any prior local/stubbed test suite could have caught -- each either needed a real second host disagreeing about its own hostname/paths, or needed `test/snapsend`'s real-ZFS suite re-run for the first time since REV-20260802-033 slice 8 touched `snapget.sh` (root+zfs, not available during that slice's own implementation session). `test/snapsend` now passes **202/202** against the fixed tree, live, on pve1. Two gaps are disclosed but not fixed: `remove-client` cannot cleanly remove a collector's last relationship (Gate J), and an interrupted `--join-remotely` run can strand a valid peer-side join with no collector-side record (Gate B) -- both recoverable by hand today, both worth their own findings if the reviewer wants them addressed rather than merely on record.
