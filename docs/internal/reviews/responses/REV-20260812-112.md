<!-- rev: REV-20260812-112 -->
<!-- response-status: IMPLEMENTED -->
<!-- implementation: 0697f01efca3636d5f9ed816595555e730db5efe -->

# Response to REV-20260812-112

## F1 — IMPLEMENTED — the Usage text now describes the verb it delivers

ACCEPTED without reservation. You are also right that recording the defect is not
fixing it; the declaration in `PROJECT_STATUS.md` existed so the gap was visible
during review rather than discovered in it, and it was never a substitute for the
correction.

### The correction

The existing Usage text, corrected in place. No new abstraction, no state token,
no documentation framework:

```
  zfs-backup.sh --source=DATASET --target=DATASET [--profile=NAME] [--config=FILE] [--install] [--yes|-y]
                                    LOCAL backup ('local-backup ...' is an alias).
                                    without --install: plans and previews, installs nothing
                                    --install:         seed first, then install the cron transactionally
                                    --yes | -y:        skip the interactive confirmation of that install
```

### Proof

Two assertions in `test/localbackup`, because one does not catch this defect's
real shape:

1. the Usage advertises `--install` and `--yes`/`-y`;
2. the old unconditional `plan/preview only` claim is **gone**.

A Usage that merely listed the new flags while still calling the command
preview-only would satisfy (1) and go on misleading exactly as before. (2) is the
discriminating half.

It earned its place immediately: my first wording kept the phrase inside the
conditional branch (`without --install: plan/preview only, …`) and assertion (2)
failed on it. The text was reworded rather than the assertion loosened.

The already-existing regression that no `--install` still plans only is untouched
and still green.

### Results

- `test/localbackup` **52/52** (50 before, +2 here) — pve1;
- `test/zfsbackup` **401/401** — pve1;
- no live-host proof, as you specified: this is a text contract.

## A note on the two commits

The fix landed on `main` as `8120d67` + `0697f01`, both titled `WIP`. A `git push`
during the step that was meant to collapse them into one message published them
first, and this project does not rewrite published commits. The content is the
reviewed change and nothing else; the reasoning that should have been in those
messages is above. My handling error, recorded rather than tidied away.

## Blocking process defect — REV-112 is invisible to the generator

`./test/reviewctl.sh --generate` cannot see this REV. It still reports 47 reviews
and produces no row for `REV-20260812-112`, so the ledger says nothing is routed
to Claude while your REV file says `routing: Claude`.

Cause: `docs/internal/reviews/REV-20260812-112.md` uses YAML front matter

```
---
review-id: REV-20260812-112
status: CHANGES-REQUIRED
reviewed-implementation: da3e8318...
routing: Claude
---
```

while `hdr()` in `test/reviewctl.sh:41` parses the documented HTML-comment form:

```
<!-- rev: REV-20260812-112 -->
<!-- verdict: CHANGES-REQUIRED -->
<!-- reviewed-implementation: da3e8318... -->
<!-- severity: P2 -->
<!-- response: docs/internal/reviews/responses/REV-20260812-112.md -->
```

Every other REV in the tree uses the second form. I have not touched your file —
it is reviewer-owned — and this response deliberately uses the HTML-comment
headers so at least my half stays machine-valid.

This is the third routing freeze from a header defect today (REV-082's verdict
line historically, REV-102's closure this morning, this one now), which is why
V2.1 delta 5 carved out the pre-publication `reviewctl --generate` check as the
one piece of machinery worth adding. That check would have caught all three
before the push. If the YAML form is a deliberate V2.2 change rather than a slip,
say so and I will implement the parser side — but the generator and the artifacts
have to agree on one form, and right now they do not.

## Remaining risk

None for F1. The routing defect above is not mine to fix and is not a product
risk; it only means this response will not appear in the generated ledger until
the REV file's headers match the parser.

Not claiming closure.
