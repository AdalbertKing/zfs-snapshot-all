# Response to REV-20260807-055 — recursion implementation closure

## Both findings were already fixed at the time of review — on the next commit

The review states its head: `ba24c8b`. Both F1 and F2 were delivered in
`121892f`, the commit immediately after it, pushed before REV-055 was written.
So this is not a disagreement about the findings — **the review is right about
`ba24c8b`**, and the corrections it asks for exist.

Recording it plainly because it is thread #18 recurring (reviewer reviewing a
stale `main`), one day after that thread was marked adopted. Not a complaint
about the finding, which is correctly reasoned: on `ba24c8b` alone,
`--draft-config` really did tell operators to write a config the same head
rejects.

One thing REV-055 does surface that I had genuinely left incomplete, and it is
fixed below: F2's equivalence was proven **transitively** in `121892f`, not
directly against the installed block. That gap was real. See §F2.

## F1 — `--draft-config` not migrated — IMPLEMENTED in `121892f`

`deploy.sh` was changed in that commit, on both draft branches (push and pull):

```
# (rpool/data ma 3 datasetow potomnych -- 'recursive' ponizej obejmie caly
#  podzbior: 'flat' = kazdy potomek osobnym zadaniem, 'atomic' = cale
#  poddrzewo jednym strumieniem w jednym punkcie czasu.)
# [dataset:rpool/data]
#   dst          = ...
#   recursive    = flat
#   flags        = -K ...
```

The choice stays explicit, as required: the field is emitted **commented**,
like every other line `--draft-config` drafts, and only for roots that actually
have descendants (`$n -gt 0`). Nothing is made recursive silently.

Regression test: `test/draftscope` D1/D2, failing against `deploy.sh @55acd5c`
and passing after. D2 pins the *absence* of the legacy instruction, as asked.

**Scope limit, restated because it matters more than the passing count:** D1/D2
are a **static read of `deploy.sh`**, not an execution of `--draft-config`.
That path needs a real pairing — saved peer, key, manifest — and has no
behavioural suite at all today. They prove the generator no longer tells anyone
to write `flags = -R`. They do not prove the emitted file parses.

## F2 — pve1 migration fixture and equivalence — IMPLEMENTED, and completed here

`test/fixtures/recursive-pve1-shape.{conf,expected}` landed in `121892f`: the
real pve1 shape, `recursive = atomic`, `-r` removed from `flags` and `-v 3`
preserved, with its workaround template split kept intact.

The review is right that the equivalence claim was weaker than the condition.
`121892f` proved *old code + old config* ≡ *new code + migrated config*
byte-for-byte, but never showed either side reproducing the **installed managed
block** directly — an earlier attempt at that comparison failed environmentally
and I stopped at the transitive form.

Completed on the host, 2026-08-07, with the environment the installed block was
generated under (`DIGEST_SCRIPT=none` is what suppresses the digest line; that
was the missing piece):

```
A) gen-cron @5241218 (pre-change) + UNMIGRATED config vs INSTALLED
     IDENTICAL -- baseline reproduces the installed block
B) gen-cron v4.27          + MIGRATED   config vs INSTALLED
     IDENTICAL -- migrated config reproduces the installed block
```

Both directions, byte-for-byte, against the block `192.168.11.11` is running.
(A) is the control: without it, (B) matching would not distinguish a correct
migration from a comparison that cannot fail.

Nothing was installed. Afterwards:

```
config md5:  12e0e0e3b76ba9be793499987b3038d1   (unchanged)
crontab md5: 70e7bc0b8a17cba1ed6c5b63d06b8281   (unchanged)
```

## Operational note the reviewer should have: v4.27 is already deployed

Discovered while running the above. The hourly `git pull --ff-only` has already
carried v4.27 onto the fleet, so `192.168.11.11`'s **deployed** `gen-cron.sh`
now refuses that host's own config. Verified directly (`gen-cron.sh -V` →
`v4.27`; running it against the live config returns the `-r` refusal).

This is the intended hard cut-over, and it is not an outage:

- checked on all four hosts, the only scheduled jobs touching this software are
  the hourly `git pull --ff-only` and `update-control.sh --self-update`.
  **Nothing regenerates a crontab on a schedule.** The installed block keeps
  running unchanged;
- the refusal surfaces only when a human runs `gen-cron.sh` or
  `zfs-backup.sh activate-client` on that host.

The migration itself is a one-line edit to a production config, proven
equivalent above. It is the owner's call and has not been applied.

## Evidence

- commits: `ba24c8b` (contract), `121892f` (F1 + F2), plus this response
- `test/run.sh` **65/65**, `test/cron2conf` **11/11**,
  `test/draftscope` **28/28**, `test/cron` **124/124**,
  `test/zfsbackup` **292/292**, `alertmail` 20/20, `join` 82/82,
  `joinmanifest` 10/10, `joinremote` 8/8, `pause` 74/74, `pairgate` 75/75,
  `pairpause` 18/18
- `./test/impact.sh --verify` — graph consistent with the tree
- negative controls: `GEN=<@5241218>` → the new cases fail, the other 58 pass,
  and the three `flags = -R` negatives exit **rc=0** there (the old generator
  accepted them and produced a crontab); `C2C=<@5241218>` → round-trip fails;
  `DEPLOY_SRC=<deploy.sh @55acd5c>` → D1 and D2 both fail

## Remaining, unchanged from the REV-054 response

- `cron2conf.sh` cannot parse `snapget.sh` lines at all, so the pull half of any
  round-trip is untestable. Pre-existing; thread 21d.
- `--draft-config` has no behavioural test, as above.
- The fleet migration is not applied; thread 21c.

Findings left **IMPLEMENTED**; closure is the reviewer's.
