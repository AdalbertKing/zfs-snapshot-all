<!-- rev: REV-20260811-101 -->
<!-- verdict: APPROVED -->
<!-- reviewed-implementation: f27fcc3293828a39c831f255ec69838a0c36a9bf -->
<!-- severity: P1 -->
<!-- response: docs/internal/reviews/responses/REV-20260811-101.md -->

# REV-20260811-101 — Phase 5 multi-source WHAT contract

Review time: 2026-08-11 08:03 CEST
Reviewed implementation: `f27fcc3293828a39c831f255ec69838a0c36a9bf`

## Reviewer determination

**APPROVED.**

Independent inspection confirms the Phase 5 public entrypoint now implements the owner-approved explicit multi-source set semantics without broadening the architecture.

The canonical `--source=a,b,c` form is normalized into an explicit root list; repeated `--source` flags accumulate into the same set rather than silently replacing earlier values; exact duplicates collapse deterministically; every root is validated before candidate composition; one missing/invalid root refuses the whole request; parent/child overlap within the requested set refuses; and unrelated roots remain independent `[dataset:root]` entries. The generated cron carries all same-policy roots in the established comma-joined `snapsend.sh` form.

REV-097/098 properties remain intact: candidate composition is additive over the existing CONFIG, existing coverage overlap is checked against every requested root and target, the bare `--source/--target` entrypoint remains canonical, and the operation is still read-only planning.

The targeted regressions are discriminating against the old scalar parser: comma-list input and repeated flags fail under the reviewed predecessor but succeed with the new set semantics; missing-member and parent/child negative controls prove fail-closed behavior rather than permissive parsing.

No frozen engine changed and no crontab/install/runtime transfer boundary changed. Therefore a live ZFS transfer is not required to close this planner-only finding. The actual multi-root transfer plus transactional install/read-back remains a Phase 5 slice 2 live-host obligation.

REV-101 no longer blocks slice 2.
