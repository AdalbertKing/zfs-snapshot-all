<!-- rev: REV-20260810-091 -->
<!-- verdict: APPROVED -->
<!-- reviewed-implementation: 05816e808a97fc6c49ef4842a50b136b1231322f -->
<!-- supersedes: - -->

# REV-20260810-091 — endpoint-only reactivation still mutates or refuses on non-topology CONFIG policy

Review opened: 2026-08-10 08:39 CEST
Final verification: 2026-08-10 10:18 CEST
Response path: `docs/internal/reviews/responses/REV-20260810-091.md`

## Verdict

**APPROVED on implementation `05816e808a97fc6c49ef4842a50b136b1231322f`.**

The submitted correction satisfies both findings without adding normalization or migration machinery.

## F1 — resolved — pure endpoint reactivation no longer re-adds CONFIG-wide `[excluded:]` policy

`ensure_cron_config()` now places the reserved-prefix `[excluded:]` append loop behind `needs_profile=1`. An ordinary reactivation with `PLAN_NEEDS_PROFILE=0` therefore preserves a deliberately customized installed CONFIG instead of silently restoring policy scaffolding.

The regression crosses the real `cmd_activate_client()` boundary, proves the removed floor stays removed while `src` refreshes, and retains a positive guard proving a genuine policy-creating call still installs the safety floor.

## F2 — resolved — pure endpoint reactivation no longer forces pre-GFS migration

`detect_profile_gfs()` still runs unconditionally, but the pre-GFS refusal now fires only when `needs_profile=1`, i.e. when policy would actually be generated. A valid installed pre-GFS CONFIG can therefore refresh endpoint topology without being forced through `migrate-profile`, while the double-prune safety refusal remains at the additive-policy boundary.

The regression validates the legacy fixture with real `gen-cron.sh`, crosses real `cmd_activate_client()`, proves `src` refresh plus unchanged legacy `prune_schedule`, and proves the policy-generating legacy path still refuses.

## Evidence reviewed

Claude reports:

- `bash test/zfsbackup/run.sh` -> **346/346**;
- negative control against `e26adc57ab0526a5e3a359de7032c5ecb6522566` -> **343/346**, with exactly the three new discriminating assertions failing;
- `./test/impact.sh --verify` -> PASS.

I independently inspected the exact production diff and the section-51 regression logic. The execution environment available to this reviewer cannot resolve github.com from the local container, so I could not rerun the suite from a checkout; this approval therefore relies on exact-code inspection plus the repository-recorded discriminating evidence, which is sufficient for this deterministic CONFIG-composition boundary and does not require a live ZFS/SSH campaign.

## Scope

This approval is deliberately limited to REV-091. A separate Gate-2 residual exists around CONFIG-wide `[excluded:]` scaffolding during additive CREATE into an already-populated CONFIG; it is tracked separately rather than widening this review.

<!-- reviewed-by: 05816e808a97fc6c49ef4842a50b136b1231322f REV-20260810-091 -->
