<!-- rev: REV-20260809-082 -->
<!-- verdict: APPROVED -->
<!-- reviewed-implementation: aee1b2bb06c0aedbfa1375cd46f3750a70505bce -->
<!-- supersedes: - -->

# REV-20260809-082 — B1 runtime composition / impact routing

Final review: 2026-08-09 20:54 CEST
Response: `docs/internal/reviews/responses/REV-20260809-082.md`

## Verdict

**APPROVED.**

The original findings were: profile-owned native fields were being reduced through a private runtime field map; `lib-profile.sh` changes did not resolve to the `zfsbackup` runtime suite; PROJECT_STATUS contradicted the new production consumer. Follow-up verification then required the discriminating field-survival proof to cross the real `gen-cron.sh` boundary, the impact regression to use the real `-f` plan interface, and one bounded live apply/read-back/refusal proof.

## Final verification

- Production composition uses generic `profile_emit()` rather than a second policy-field allow-list; relationship-owned fields remain injected by orchestration.
- The discriminating profile is composed into a complete CONFIG and the real `gen-cron.sh` both accepts it and renders `recursive=flat` as `-R`.
- `impact.sh -f lib-profile.sh` resolves both `test/profiles/run.sh` and `test/zfsbackup/run.sh`.
- The bounded live campaign on metropolis pve1/pve2 took `v3proofA` through the real `add-client -> seed -> verify-endpoint -> activate-client` path, then captured the canonical CONFIG and managed crontab after activation.
- The corrected negative leg is documented in REV-086 and proves refusal before transfer with CONFIG/crontab/ZFS destination unchanged; no second campaign is required for this REV.
- Later overlap and CRON_CONFIG fixes do not invalidate the B1 property reviewed here; the independent CRON_CONFIG defect is tracked separately as REV-087.

No remaining acceptance criterion from REV-082 is open. Detailed earlier review text remains available in Git history; this current artifact records the final machine state and closure basis.
