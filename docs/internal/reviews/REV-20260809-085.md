<!-- rev: REV-20260809-085 -->
<!-- verdict: APPROVED -->
<!-- reviewed-implementation: a1ff3a07f5d60a1437c2799b548dc188f134688d -->
<!-- supersedes: - -->

# REV-20260809-085 — overlap refusal before real seed transfer

Final review: 2026-08-09 20:54 CEST
Response: `docs/internal/reviews/responses/REV-20260809-085.md`

## Verdict

**APPROVED.**

The finding was that overlap refusal existed only on the later activation/config path, while `cmd_seed()` could already perform a real receive into overlapping coverage. The correction in `a1ff3a07...` places the same canonical fail-closed overlap guard after final dataset resolution and before the transfer loop.

## Final verification

- Same-peer child-of-owned, parent-of-owned and exact-path cases drive real `cmd_seed()` with an observable SNAPGET recorder and require zero transfer invocations.
- A genuinely disjoint different-peer case still reaches the transfer recorder.
- The negative control against `f1c4b960...` reaches the recorder for the overlapping case, so the regression distinguishes the defect.
- No duplicate overlap implementation was introduced; seed and activation call the same `assert_no_coverage_overlap()` logic.
- The corrected live campaign documented under REV-086 uses a distinct client name sharing the same peer-derived namespace, reaches the seed-time guard, refuses before transfer, and proves no CONFIG/crontab/destination-ZFS mutation.

No remaining acceptance criterion from REV-085 is open. The separate CRON_CONFIG defect discovered during the campaign is tracked as REV-087.
