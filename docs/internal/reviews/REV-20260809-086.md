<!-- rev: REV-20260809-086 -->
<!-- verdict: APPROVED -->
<!-- reviewed-implementation: 67415a5795ad3d1cb659e3017c1485bc99b59a61 -->
<!-- supersedes: - -->

# REV-20260809-086 — live proof must reach the seed-time overlap guard

Final review: 2026-08-09 20:54 CEST
Response: `docs/internal/reviews/responses/REV-20260809-086.md`

## Verdict

**APPROVED.**

The original evidence-plan defect was valid: a same-client-name second add stops at identity uniqueness and cannot prove the seed-time overlap guard. The corrected campaign uses a distinct client record sharing the active relationship's peer manifest, so it reaches the actual `cmd_seed()` coverage preflight without a second pairing operation.

## Final verification

- `v3proofA` was taken through the real create/seed/verify/activate lifecycle and baseline CONFIG, managed crontab and ZFS destination state were captured.
- `v3proofB` had a different `CLIENT_NAME`, the same `PEER_HOST`, and therefore the same peer-derived label/manifest/destination. It entered `cmd_seed()` and failed with the canonical overlap diagnostic naming `v3proofA`.
- The refusal occurred before the transfer loop; the transcript contains no seed-transfer start for B.
- Before/after CONFIG and crontab diffs are clean and the destination ZFS listing is unchanged; no additional source snapshot attributable to B appeared.
- The active work plan has been corrected to remove the false client-name/LABEL premise.
- The two operational mistakes disclosed during the campaign are not hidden. One independent product defect exposed by the campaign (`CRON_CONFIG` clobbering) is deliberately tracked separately as REV-087 rather than being absorbed into this evidence-plan REV.

REV-086's acceptance criterion is therefore satisfied. Detailed earlier review text remains available in Git history.
