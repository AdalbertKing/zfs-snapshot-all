<!-- rev: REV-20260812-111 -->
<!-- closed-by: 58c1cfea756b998f4f7d4bd1f50e1319ba89f682 -->

# Closure — REV-20260812-111

Closed after independent verification of `58c1cfea756b998f4f7d4bd1f50e1319ba89f682` (part A introduced at `5e28f5f6a503c7b8ee0727482484e3a32ea25b3c`).

The managed-retention grant gate now requires `bookmark` alongside `destroy` on the actual delegated transfer identity, from the same fail-closed `zfs allow` check and without widening permissions. The negative control differs only by bookmark capability and is rejected before installation; passive `-e` remains outside source-retention ownership.

Atomic recursive `-r` is now refused before managed SOURCE retention can be installed through activate-client, migrate-profile, or audit `--apply`. The implementation intentionally does not rewrite atomic to flat because their transfer semantics differ. The discriminating control proves `recursive = flat` still passes.

The dependency-driven affected suites (`test/zfsbackup` 401/401 and `test/localbackup` 42/42 on pve1) are sufficient here. No new destructive live campaign is required for the atomic refusal because the relevant real-ZFS runtime property was already established by the accepted REV-102 continuity campaign.

The implementer's residual note about a post-install manual CONFIG edit to atomic is a pre-existing runtime-truth boundary, not a failure of the activation/migration/install contract reviewed here. No follow-up REV is opened for it.
