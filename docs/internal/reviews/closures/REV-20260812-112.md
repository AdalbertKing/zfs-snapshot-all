<!-- rev: REV-20260812-112 -->
<!-- closed-by: 046a82e52818904ec46129cd380b7397a9087763 -->

# Closure — REV-20260812-112

Closed after independent verification of `0697f01efca3636d5f9ed816595555e730db5efe`.

The public Usage text now advertises `--install` and `--yes|-y`, states that omission of `--install` remains plan/preview-only, and describes the transactional seed-then-install path. The targeted test covers both the newly exposed flags and removal of the stale unconditional preview-only claim; the existing no-`--install` regression remains green.

No additional live-host proof or broad regression campaign is required because this finding is limited to the CLI help contract; the runtime behavior and real-ZFS/sandboxed-crontab evidence for the transactional slice were already independently reviewed.

The reviewer-side header-format defect that temporarily broke `reviewctl --generate` was corrected by normalizing REV-112 to the canonical HTML-comment machine headers before closure.
