<!-- rev: REV-20260810-095 -->
<!-- verdict: APPROVED -->
<!-- reviewed-implementation: 8db8c4d4c4b57efcafb83fcc447ffe336ac4622b -->
<!-- supersedes: - -->

# REV-20260810-095 — Phase 4 preset selection is not yet proved through the real first-activation boundary

Review time: 2026-08-10 20:12 CEST
Reviewed implementation: `9074fe5543fa1077aa871b135c721232a2cdf0b5`
Approved implementation: `8db8c4d4c4b57efcafb83fcc447ffe336ac4622b`

## Verdict

**APPROVED — Gate 4 evidence now crosses the real first-activation boundary.**

The Phase 4 slice itself already had the correct small production surface: `add-client --profile=NAME`, early profile validation, storing the CREATE-time choice, and applying it only for a genuinely new relationship. Re-activation deliberately ignores the stored profile, preserving the one-way handoff established by Phase 3.

The original gap was evidentiary: section 53 tested `cmd_add_client()` and unit-tested `apply_client_profile_choice()` separately, never crossing the real `cmd_activate_client()` caller boundary.

## Required outcome

The requested proof was one focused automated composition test using the real `cmd_activate_client()` boundary, an alternate profile with observable semantics, the default path, reactivation preservation, a negative control for the stored-PROFILE wiring, the real generator, and only the dependency-selected local cascade. No new live ZFS/SSH campaign was required unless pairing or installation semantics changed.

## Verification of implementation `8db8c4d`

Verified independently from the current test code and diff:

1. Section 54 runs the real `cmd_activate_client()` with `STATE=endpoint_verified` and a captured candidate workfile. The `$SNAPGET` stub exits non-zero after capture, so execution stops at the existing dry-run gate before grant checks or `atomic_replace_and_install`; this is an appropriate deterministic boundary for the property under review.
2. The environment forces `PROFILE_ACTIVE=default`, while the client record carries `PROFILE=alt`. The alternate profile differs semantically by hourly `send_schedule = 7 * * * *`. The candidate must therefore traverse the stored record through `apply_client_profile_choice()` to obtain ALT semantics.
3. The positive assertion checks both ALT namespace and ALT content and rejects a default-profile leak.
4. The captured candidate is then rendered by the real `gen-cron.sh`, and the test requires the minute-7 cron line. This crosses the CONFIG-text-to-generator boundary rather than stopping at appearance.
5. The default path is rechecked with the exact stored representation produced by omitted `--profile` (`PROFILE=default`) and requires default semantics with no ALT leak.
6. The negative control replaces `apply_client_profile_choice()` with a no-op in the subshell; the same `PROFILE=alt` record then falls back to default. This directly demonstrates that the positive test is discriminating on the wiring under review.
7. Re-activation (`STATE=active`, stored `PROFILE=alt`) is re-exercised through the real caller and rejects ALT leakage, preserving the one-way handoff.
8. No production code, pairing, live host state, SSH grant, UID, ZFS, or crontab installation behavior changed in `8db8c4d`; the broader `zfsbackup-live-pair` obligation therefore remains separate and is not required to close this finding.

## Phase impact

Phase 3.5 remains CLOSED.

Phase 4 slice 1 is APPROVED. Gate 4 is satisfied for the selected-preset-to-first-activation wiring represented by `9074fe5` plus regression proof `8db8c4d`.

Phase 5 is no longer blocked by REV-095.
