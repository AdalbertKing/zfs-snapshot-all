<!-- rev: REV-20260809-089 -->
<!-- verdict: APPROVED -->
<!-- reviewed-implementation: c5f04ab0bf9c06ef4faa9e10d605aeb2cd552fc5 -->
<!-- supersedes: - -->

# REV-20260809-089 — reactivation rebuilds managed sections from the current profile and discards installed policy

Review opened: 2026-08-09 23:11 CEST
Final verification: 2026-08-10 08:37 CEST
Response path: `docs/internal/reviews/responses/REV-20260809-089.md`

## Verdict

**APPROVED on implementation `c5f04ab0bf9c06ef4faa9e10d605aeb2cd552fc5`.**

The submitted correction resolves the finding this REV filed: ordinary reactivation no longer removes and regenerates relationship-owned `[dataset:]` / `[prune:]` policy from the current profile. Existing owned dataset sections are preserved as the base and only endpoint-owned `src` and transport `flags` are refreshed; owned prune policy is left untouched.

This approval is intentionally scoped to the relationship-section clobber defect. REV-20260810-090 and the later REV-20260810-091 cover independent residual Phase-3 dependencies outside this finding.

## F1 — resolved — ordinary reactivation preserves relationship-local CONFIG policy

Independent inspection of exact implementation `c5f04ab0...` confirms:

1. first activation still takes the generation path;
2. reactivation identifies sections by the existing ownership contract rather than adopting matching headers;
3. owned dataset sections are edited in place only for `src` and `flags`;
4. `pair_label`, `notify`, policy fields, hand-added fields and comments are not regenerated;
5. owned backup-mode GFS prune sections are not rewritten;
6. sync-mode preservation requires both dataset and matching prune ownership before preserving the pair;
7. foreign/unowned sections still fall through to the existing fail-closed removal/refusal path;
8. `migrate-profile` explicitly retains full regeneration semantics.

The field boundary is justified from current runtime inputs: `src` and `flags` are the fields derived from endpoint/transport state, while `pair_label`/`notify` are stable functions of relationship identity and dataset path.

## Evidence verification

The response reports `zfsbackup` **333/333** with a negative control of **328/333** against the reviewed pre-fix state, with exactly the new discriminating assertions failing. The later REV-090 campaign, which contains this fix unchanged and crosses the real `cmd_activate_client()` boundary, reports the dependency-selected cascade and `./test/impact.sh --verify` green after the independent reviewer-owned REV-088 metadata blocker was repaired.

That later green verify is acceptable for this REV's previously blocked protocol criterion because the blocker was independently identified as reviewer metadata, not product behavior, and the reviewed `c5f04ab0...` relationship-section correction remains unchanged in the successor.

No new live ZFS/SSH campaign is required: the correction is deterministic CONFIG composition and does not alter transfer, grants, UID, real cron installation semantics or host-state mutation.

<!-- reviewed-by: c5f04ab0bf9c06ef4faa9e10d605aeb2cd552fc5 REV-20260809-089 -->
