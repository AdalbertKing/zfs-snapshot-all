<!-- rev: REV-20260812-115 -->
<!-- verdict: APPROVED -->
<!-- reviewed-implementation: d132aa1f2233258eb682d16ce4a3cd7064979d96 -->
<!-- severity: P2 -->
<!-- response: docs/internal/reviews/responses/REV-20260812-115.md -->

# REV-20260812-115 — CI graph gate cannot validate Protocol V2 from a shallow checkout

## Verdict

APPROVED at `d132aa1f2233258eb682d16ce4a3cd7064979d96`.

The implementation preserves the Protocol V2 reachability invariant and fixes only the CI checkout boundary. The `graph` job has full history where `./test/impact.sh --verify` needs it, and the matrix `impact` leg explicitly unshallows before running; the other matrix legs remain shallow.

Independent verification of GitHub Actions run `31639713240` on the exact submitted SHA shows overall `success`; specifically `dependency graph matches the tree` succeeded, `impact` succeeded, the conditional history-fetch step ran only for `impact`, and was skipped for the other matrix suites. The submitted diff does not weaken `require_commit()` or alter product/runtime code.

The implementer also reports `test/impact/run.sh` 58/58 and full-history `./test/impact.sh --verify` green on pve1. Those claims are consistent with the exact CI evidence above; no ZFS/SSH/live-host product campaign is relevant to this CI-only boundary.

## Resolution of F1

Resolved. The false-red shallow-checkout condition is removed without reducing review-SHA verification strength. No further finding is opened from this review.
