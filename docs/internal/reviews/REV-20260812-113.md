<!-- rev: REV-20260812-113 -->
<!-- verdict: APPROVED -->
<!-- reviewed-implementation: 5ede32de9c8ba7c3d76d0757898267749abc8055 -->
<!-- severity: P1 -->
<!-- response: docs/internal/reviews/responses/REV-20260812-113.md -->

# REV-20260812-113 — Phase 7 slice 1 restore planner

## Verdict

APPROVED — F1 is resolved at `5ede32de9c8ba7c3d76d0757898267749abc8055`.

Independent diff verification confirms the correction is narrow: the planner now names `-d 1` explicitly on its snapshot listing, and `test/restore` rejects snapshot-list invocations that omit that selector. No unrelated restore behavior was added.

The live evidence on pve1 (`zfs-2.1.9-pve1`) is sufficient for the environment-dependent boundary that triggered this review:

- exact pre-fix command against a disposable dataset with one snapshot returned that snapshot with rc=0;
- a disposable dataset with no snapshots returned an empty listing with rc=0;
- a parent dataset with a snapshotted child returned only the parent's snapshot, excluding the child's;
- the same selector with explicit `-d 1` produced the intended result;
- the pool had `listsnapshots=off`, while explicit `-t snapshot` still enumerated the recovery point.

This proves the original implementation was not false-negative on the deployed OpenZFS version, while the explicit depth selector removes unnecessary dependence on an implicit default. The strengthened stub is the durable regression: removing `-d 1` now causes the restore snapshot path to fail rather than silently blessing an unproven command shape.

Targeted evidence is proportionate to the change: `test/restore` 13/13 plus the dependency-selected `localbackup` 57/57 and `zfsbackup` 401/401 on pve1. A broader campaign or another live-host run would not add meaningful evidence for this selector-only change.

Phase 7 slice 1 is reviewer-clean. No further finding remains in REV-113.
