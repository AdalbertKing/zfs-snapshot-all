<!-- rev: REV-20260813-119 -->
<!-- verdict: CHANGES-REQUIRED -->
<!-- reviewed-implementation: eb7cf11528e1b9f7b72efd5cfc760dd34593f640 -->

# REV-20260813-119 — Phase 7 destructive-recovery gates

Reviewed implementation: `eb7cf11528e1b9f7b72efd5cfc760dd34593f640`
Reviewer verdict: **CHANGES REQUIRED**

The internal-only placement is correct: this delivery does not freeze the Owner's still-open public Restore grammar. `restore_relations()` also usefully gives planner and future execution one relationship resolver, and the current slice itself does not mutate ZFS.

## F1 — P1 — confirmation is specified before the exact live-loss set is captured

The revised `docs/design/destructive-recovery-contract.md` specifies this order for the future destructive path:

1. resolve / preview;
2. explicit destructive confirmation;
3. snapshot the live source as a measurement;
4. rollback/discard and receive.

That ordering does **not** satisfy the authoritative Owner property: show exactly what will be lost **before** destructive confirmation.

REV-118 already proved why: `written=0` can lag the open txg. A live write can therefore exist without appearing in the read-only preview. The proposed post-confirmation snapshot would capture that write only after the operator has already approved destruction. It improves the after-the-fact measurement, but it cannot retroactively make the confirmation informed.

There is also a TOCTOU form of the same defect: even when the preview was accurate at time T1, the source can acquire a new write or snapshot before the destructive boundary. Destroying it without a fresh, operator-visible loss set would discard state that was never approved.

### Required property

Before any destructive execution slice can land:

- the exact state that would be discarded must be captured/measured and shown **before** confirmation;
- the implementation must establish a commit-boundary check so that a source change after the preview/confirmation cannot be silently included in the destruction;
- if the source changes after the approved preview, fail closed and re-preview/re-confirm rather than destroy the new state;
- preserve the Owner's zero-choice policy: do not reintroduce `preserve-source` vs `discard-source-changes` or transport-policy choices;
- this fix remains below the public CLI boundary while the grammar discussion is open.

A pre-confirmation technical snapshot may be one possible implementation technique, but it is not automatically sufficient: the code still needs to prove that state created after that snapshot cannot cross the destructive boundary unnoticed. Do not call a temporary snapshot a preserved/safety copy if the restore will destroy it.

### Reviewer follow-up after C-020 design — residual race remains

C-020 improves the ordering materially: a technical snapshot before confirmation can turn the previously unaccounted live delta into an exact, operator-visible fact, and a second technical snapshot can detect changes that arrived between the approved preview and that second snapshot.

It still does **not** yet prove the required property at the destructive commit boundary.

The proposed sequence is effectively:

1. preview snapshot P1;
2. show P1-derived loss set;
3. operator confirms;
4. boundary snapshot P2;
5. verify no state arrived between P1 and P2;
6. rollback/destruction.

A write can arrive **after P2/check and before step 6**. That write is outside the approved loss set and would still be destroyed by rollback. P2 therefore closes the race only up to P2; it does not fence the source for the remaining destructive transition.

The implementation needs an actual write/execution fence or an equivalent atomic construction before destruction. The exact mechanism is an implementation choice, but acceptance requires proof that, after the final approved-state validation, no new source state can appear and be destroyed under the earlier confirmation. If the fence itself changes the source or operational availability, preview/output and cleanup semantics must say so explicitly and fail closed on inability to establish/release it.

Do not solve this by adding an ordinary-user policy flag. This is execution safety under the existing zero-choice intent.

### Regression evidence required

Add tests that distinguish both dangerous races from the clean case:

- state appears after the first preview but before the commit-boundary validation → refuse/re-preview;
- state attempts to appear after the final validation but before rollback/destruction → it must be prevented/fenced or otherwise provably unable to be silently destroyed.

The test must fail if a post-confirmation snapshot merely measures newly arrived state and then destroys it.

## CI gate note

The earlier exact routing head `f571917234bcb98c7cde306563b093460e4387cd` was RED because `test/tune/run.sh` failed two probe assertions. On later exact head `58ba86c2c2452496051b814c884c4374cc71982a`, `tune` is GREEN; the remaining `impact` / dependency-graph failures are routing drift because REVIEW_LEDGER / OPEN-THREADS have not yet been regenerated to include REV-119 OPEN → Claude. Closure still requires a green exact implementation/routing head.

## Scope

No finding is raised against the still-open public selector/destination grammar. Keep that surface unfrozen until the Owner/Claude/Reviewer discussion reaches a durable conclusion.
