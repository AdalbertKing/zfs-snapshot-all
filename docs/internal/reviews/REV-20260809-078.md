<!-- rev: REV-20260809-078 -->
<!-- verdict: APPROVED -->
<!-- reviewed-implementation: dc1c038e262fb4a5965e3161a79f916b510b9d76 -->
<!-- supersedes: - -->

# REV-20260809-078 — `PROJECT_STATUS` current-state is stale despite a current digest

Review time: 2026-08-09 00:57 CEST

## Verdict

CHANGES REQUIRED.

This is a new, independent documentation/process finding. It does not reject the Slice B1 plan itself.

## F1 — the document's own current-state block contradicts the current tree

`docs/PROJECT_STATUS.md` says, at the top:

- `Data odświeżenia: 2026-08-07`;
- `gen-cron.sh v4.30`;
- the engine/profiles/restore note is only discussion and **"Nic z długich opcji rekursji, profili ani restore nie jest zaimplementowane."**

That is no longer a truthful summary of `main`.

Concrete counterexamples at the reviewed implementation:

1. Stage 2.3 long recursion spellings are shipped in `snapsend.sh` (`--recursive=atomic|flat|no`).
2. Stage 3 engine freeze is shipped and closed.
3. Stage 4 reconcile is shipped and closed through REV-074.
4. The profile boundary is now production code in `lib-profile.sh`; the same `PROJECT_STATUS.md` later records the `profiles` suite as 39/39 after REV-076/077. Runtime profile consumption is **not** shipped yet, but saying nothing from profiles is implemented is false.
5. Slice B1 is now planned and unblocked, while profile runtime consumption is still pending. That distinction is the current state the top summary should communicate.

The dangerous part is that the machine freshness marker is current enough to let `impact.sh --verify` pass while the prose summary is stale. This limitation was already acknowledged in commit `76bcc4d` (the digest proves synchronisation, not semantic truth). Here it has happened again in the one block the file's own header says must be current.

So this is not a complaint about the digest implementation. The digest is doing its documented job. The defect is that the human current-state block was not refreshed when later PROJECT_STATUS edits refreshed the digest and lower sections.

## Required correction

1. Refresh the **leading current-state block** so it truthfully describes the current tree.
2. At minimum distinguish clearly:
   - Stage 2.3 recursion CLI: shipped;
   - Stage 3 freeze: shipped/closed;
   - Stage 4 reconcile: shipped/closed;
   - Stage 5 profile boundary/fixtures: shipped;
   - Stage 5 runtime profile consumption: not shipped yet; Slice B1 is the next implementation slice;
   - single-host high-level orchestration: design discussion only, not shipped;
   - restore: not shipped.
3. Remove or rewrite the stale sentence claiming that none of long recursion options/profiles/restore is implemented.
4. Preserve historical material as history; do not rewrite old entries to look current.
5. Avoid creating a second independently-maintained copy of volatile details where a short accurate summary plus pointers to canonical Stage/REV artifacts is enough.

## Proof / test scope

This is documentation correctness, not an engine change. No broad runtime campaign is required.

Required:

- read the corrected leading block against current `main` and the canonical REVIEW_LEDGER / Stage plan;
- regenerate/refresh the project-status marker using the normal `impact.sh --refresh-status` workflow if the staged tree requires it;
- `./test/impact.sh --verify` clean;
- report the exact implementation SHA.

Do not touch frozen engine files for this finding.

## Reviewer follow-up — APPROVED

Review time: 2026-08-09 02:04 CEST

Reviewed implementation: `dc1c038e262fb4a5965e3161a79f916b510b9d76`

**Verdict: APPROVED.**

I independently inspected the exact implementation diff and the current leading block. It now distinguishes the shipped Stage 2.3 recursion CLI, closed Stage 3 freeze, closed Stage 4 reconcile, production profile boundary, not-yet-shipped profile runtime, not-yet-shipped single-host orchestration, and not-yet-shipped restore. The stale blanket sentence is retained only as explicitly dated 2026-08-07 history, so it can no longer be read as current state.

The response reports `./test/impact.sh --verify` clean and no frozen-engine changes. I cannot independently re-run the repository shell tests from this reviewer runtime because it has no direct GitHub network checkout; for this documentation-only finding, the exact diff plus current-tree inspection is sufficient to verify the acceptance property that was actually under review.

Minor wording note, not a blocker: the current-state table still mentions `add-local` parenthetically while the newer local-UX discussion has struck that public verb out. The row itself unambiguously says the capability is **not implemented** and points to the live design documents, so this does not recreate F1 or justify keeping REV-078 open. It can be cleaned up with the next ordinary PROJECT_STATUS refresh.

No REV-078 finding remains open.
