<!-- rev: REV-20260829-126 -->
<!-- verdict: CHANGES-REQUIRED -->
<!-- reviewed-implementation: 3fd4512bc41d65696363c7ad35ec018f8536ef19 -->
<!-- supersedes: - -->

# REV-20260829-126 — source-only no-work gate can leave a rotated medium stale

Review date: 2026-08-29
Reviewer: ChatGPT
Reviewed delivery: PR #181, merge `9b442ed459dc4792da9e62ab9e8eb286638da87c`
Finding introduced by: `a0ed00d7cf50a751b8fb0d0089ed800ad45c99e`
Current canonical descendant checked: `23be8e59610746dca50acf71024425ea7e3917b3`

## Verdict

**CHANGES REQUIRED — P1: the new pre-import optimisation can skip a stale
rotated replica because it proves only that the source is quiet, not that the
medium in the slot is current.**

The safety work around bounded export, ownership markers, pull warnings and the
nightly default is directionally sound. The blocking defect is confined to the
new `attach` fast path that returns before importing the medium.

## F1 — P1 BLOCKING — `written@latest-source-snapshot == 0` is not a fact about this medium

### Evidence

`zfs-media-gate.sh` now runs the no-work test before it asks whether the pool is
imported. For every source dataset it selects the newest source snapshot whose
name matches the replica prefix and reads `written@<snapshot>`. If all values
are zero, it returns `1` and deliberately never imports the medium.

At that point the gate has read no fact from the target. It does not know the
medium GUID, its newest snapshot, or which bookmark/base it last received. A
quiet source proves only that nothing changed after the newest source snapshot;
it does not prove that this particular rotated disk contains that snapshot.

The normal two-medium sequence is enough to reproduce the false skip:

1. medium A is synced through `replica_s1` and exported;
2. medium B is inserted and synced through the newer `replica_s2`;
3. the source remains unchanged after `replica_s2`, so
   `written@replica_s2 == 0`;
4. medium A returns. The new pre-check sees zero on the source, exits `1`, and
   never imports A, although A still ends at `replica_s1` and lacks `s2`.

Every later run repeats the skip while the source is quiet. On an archival or
otherwise stable dataset the older off-site copy can therefore remain stale
indefinitely, with a log claiming there was nothing to copy.

The green L1 case in `test/mediagate/run.sh` models only one source-side
snapshot and one byte count. It contains no per-medium target state, so its
`101/0` result proves the branch executes but cannot distinguish a current
medium from a rotated stale one. This is the same missing discriminator as the
defect.

### Acceptance criteria

1. A no-work decision must be specific to the medium that would be skipped.
   If that medium is exported and its current replica identity cannot be proved
   without importing it, fail open and import it. A durable outside-the-medium
   record is acceptable only if it is keyed by stable pool GUID and advanced
   solely after a verified successful transfer.
2. Add the discriminating A/B rotation case: A ends at `s1`, B advances the
   source family to `s2`, the source is quiet after `s2`, then A returns. A must
   be imported and brought to `s2`. The case must fail against
   `a0ed00d7cf50a751b8fb0d0089ed800ad45c99e` for the false early skip.
3. Preserve the useful optimisation for a medium independently proved current:
   current medium at `s2` plus zero source writes may skip without opening the
   import window.
4. Missing, unreadable, stale or contradictory per-medium evidence fails open
   to the real engine; it must never be interpreted as “current”.
5. Run the targeted `mediagate` suite, `./test/impact.sh --verify`, and exact-head
   CI. No additional replica modes, GUI surface or protocol machinery belongs
   in this correction.

## Sequence after this correction

REV-123, REV-124 and REV-125 have passed independent review and are being
closed separately. REV-126 is the only new blocker found in the post-#166
replica additions. Fix this bounded defect, close Stage 0, then return to the
published schedule: Stage 1 Restore, Stage 2 Profiles, and only afterwards more
replica/GUI development.

## Required response

Create one response at
`docs/internal/reviews/responses/REV-20260829-126.md`. Submit one exact
implementation SHA reachable from canonical `main`, explain the per-medium
proof used by the fast path, and include the A/B negative control plus the
targeted results above.

## Review of implementation `3fd4512bc41d65696363c7ad35ec018f8536ef19`

Review date: 2026-08-30
Submitted through: PR #185, merge `00ac066da2ee94fdaa50d888e32760f81dd18331`;
SHA stamped by PR #186, canonical `main`
`e0955281ca7be33a69a1634660ba10e1d632490a`

### Verdict

**CHANGES REQUIRED — F1 remains open.**

The submitted implementation fixes the original one-medium A/B discriminator:
a recorded stale snapshot imports, a different GUID imports, and a single
medium independently proved current may skip. The new live A/B evidence is
consistent with those branches. Exact-head CI is green, the unmodified targeted
suite reports `109/0`, and `./test/impact.sh --verify` passes.

It does not satisfy criterion 4 when the pre-import scan returns two pools with
the configured name. `scan_guid()` prints the first matching `id:` and exits.
The fast path can therefore declare that first GUID current and return `1`
before execution reaches the existing candidate-count guard, whose contract is
to refuse two same-named rotated media rather than choose one.

This is contradictory per-medium evidence, not a current-medium proof. It is
also an ordinary operator error already covered by the gate's safety contract:
two rotated disks may be connected at once. If the first scan entry matches the
record while the other disk is stale, the run reports `SKIPPED: ... Nothing to
copy` and neither reaches the required ambiguity refusal nor brings the stale
copy forward.

### Discriminating reproduction

Against exact submitted SHA `3fd4512bc41d65696363c7ad35ec018f8536ef19`,
extend the existing section L harness by keeping the source quiet and the
current record unchanged from L1, but expose two candidates:

```sh
IMPORTABLE="rotpool rotpool"
out="$(g attach rotpool rep --dataset rotpool/replica \
    --source tank/src --prefix replica_)"; rc=$?
check "two same-named visible media remain ambiguous" "2" "$rc"
has "2 pools named" "$out"
```

Observed:

```text
want [2] got [1]
SKIPPED: this medium (guid 11111111) already holds 'replica_s2' ...
PASS=109 FAIL=2
```

Without that reviewer-only control, the same checkout reports
`mediagate 109/0`; the control is what distinguishes preserving the old
ambiguity invariant from silently bypassing it.

### Required correction

1. The pre-import fast path must count matching import candidates before it may
   use any GUID. Zero candidates remains absence, exactly one candidate may be
   compared with the durable record, and more than one must preserve the
   existing `ambiguous` refusal (`exit 2`). It must not pick the first scan row.
2. Add the two-same-name case above to `test/mediagate/run.sh`: quiet source,
   apparently matching recorded GUID/snapshot, two visible candidates. It must
   refuse, import nothing, and must not emit the no-work `SKIPPED` result.
3. Preserve the now-correct A/B stale-medium case, the single-current-medium
   optimisation, the failed-transfer no-advance rule, and the existing
   wrong-medium/ownership checks.
4. Re-run only `test/mediagate/run.sh`, `./test/impact.sh --verify`, and exact-head
   CI. No broader Restore, Profiles, replica or GUI work belongs in this retry.
