<!-- rev: REV-20260813-117 -->
<!-- verdict: CHANGES-REQUIRED -->
<!-- reviewed-implementation: 1f8eb8d04535446e22749613310db977659743e0 -->

# REV-20260813-117 — stale canonical state

Severity: P2 process/auditability
Routing: Claude
Response: `docs/internal/reviews/responses/REV-20260813-117.md`

## F1 — PROJECT_STATUS freshness contradiction

Current `docs/PROJECT_STATUS.md` says `Data odświeżenia: 2026-08-11` and explicitly says an older date than the last behavior-changing commit is a defect. Phase 7 behavior changed on 2026-08-12, including planner-map `306cdba58628b49e2e4d203a80360b487facb154`.

Refresh the human-facing status for the current Phase 7 state and refresh/prove the status marker against the prospective tree with the repository's existing impact/status verification. No broad product suite is required.

## F2 — review routing is stale; reviewer-owned REV-116 header mismatch has been corrected

The first REV-117 draft described this only as an unregenerated ledger. Exact CI inspection showed the deeper cause: the REV-116 response correctly changed its machine `implementation` to evidence submission `6ebb1d45257dd05994f1140d3f644525aac7bd5b`, but my APPROVED review still named product SHA `306cdba...` as `reviewed-implementation`. `reviewctl` therefore correctly classified REV-116 as INVALID; merely generating would have persisted that invalid state.

That was a reviewer-owned metadata defect, not implementer work. I corrected REV-116 in reviewer commit `5e3a686d02d16b76caae93ab838feba2e1ecab6e`: its approval now names evidence submission `6ebb1d...` while the prose explicitly records that the unchanged product implementation under evidence is `306cdba...`.

After refreshing PROJECT_STATUS, regenerate `REVIEW_LEDGER.md` and `OPEN-THREADS.md` from the corrected canonical headers and verify with `./test/reviewctl.sh --verify` plus the minimum status/impact verification required by the repo. Do not hand-edit the generated routing.

## Required proof

1. refreshed PROJECT_STATUS human-facing date/state;
2. refreshed status marker and minimum status/impact verification required by the repo;
3. `./test/reviewctl.sh --generate` then `./test/reviewctl.sh --verify`;
4. resulting REV-116 state is CLOSED, REV-117 routes correctly during the response, and no INVALID review remains;
5. resulting `OPEN-THREADS` state.

No ZFS/SSH/crontab/deploy/live-host proof is required; this finding is canonical-state bookkeeping only. The Phase 7 planner product behavior accepted under REV-116 remains accepted.
