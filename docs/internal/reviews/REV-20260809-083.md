<!-- rev: REV-20260809-083 -->
<!-- verdict: APPROVED -->
<!-- reviewed-implementation: aee1b2bb06c0aedbfa1375cd46f3750a70505bce -->
<!-- supersedes: - -->

# REV-20260809-083 — create-only overlap refusal

Final review: 2026-08-09 20:54 CEST
Response: `docs/internal/reviews/responses/REV-20260809-083.md`

## Verdict

**APPROVED.**

The original finding required a fail-closed high-level refusal for exact/parent/child coverage overlap before candidate CONFIG/crontab mutation. `57deecb...` supplied the conservative two-way prefix guard and REV-084 closed the unreadable/unparseable-record fail-open gap.

A later independent review (REV-085) found an earlier data-plane boundary: seed could receive before the activate-time guard. That is now corrected by the same canonical `assert_no_coverage_overlap()` call in `cmd_seed()` before the real transfer loop.

## Final verification

- Parent→child, child→parent and exact overlap are covered by discriminating local regressions; disjoint coverage remains allowed where the peer/target namespace is genuinely disjoint.
- Damaged/nameless relationship state remains fail-closed through the REV-084 correction.
- `emit_client_sections()` still checks before the first candidate CONFIG mutation.
- `cmd_seed()` now checks final resolved local destination paths before any real `snapget` invocation.
- Live campaign: active `v3proofA`; distinct `v3proofB` reused the same peer manifest, reached `cmd_seed()`, and was refused by the overlap guard naming `v3proofA` before transfer. Canonical CONFIG, managed crontab and destination ZFS subtree were read back unchanged.
- The old statement that different client names imply different backup LABEL namespaces was correctly removed from the active plan; LABEL is peer-derived.

No remaining REV-083 criterion is open. Detailed earlier review text remains in Git history.
