# Open threads

One table, three readers: owner, reviewer, implementer. If a thread is not
here, nobody is tracking it. Updated by whoever moves a thread.

Last update: 2026-08-07, after reviewer approval of REV-054 recursion design.

| # | Thread | State | Whose move |
|---|---|---|---|
| 1 | REV-045 — logical pause | **CLOSED** by the reviewer 2026-08-06 for the declared scope. Endpoint-switch and in-flight live cases explicitly not required for closure. | — |
| 2 | REV-046 — alert-delivery false health | CLOSED by the reviewer | — |
| 3 | REV-047 — pair-gate audit logging | **CLOSED** (F1 and F2) by the reviewer 2026-08-06. | — |
| 4 | Hard disable — the package | **CLOSED** by the reviewer 2026-08-07 (`REV-20260807-052`, APPROVED). No code or test finding open. Approval is for the documented enforcement-gate model, with the self-enable limitation stated. | — |
| 16 | REV-049 — ownership must fail closed | **CLOSED** by the reviewer 2026-08-06. | — |
| 17 | REV-050 — state-dir group digit | **CLOSED** by the reviewer 2026-08-06. | — |
| 5 | ADR-0012 wording vs what was built | **Done** 2026-08-06: "security gate" is now "enforcement gate", with the self-enable consequence written into the ADR. Code and key model unchanged, as instructed. | — |
| 6 | Test policy | **Settled**: `TEST-CAMPAIGN-POLICY.md` accepted by the reviewer as the single source of truth; issue #5 is the record of the reasoning, not a parallel spec. | — |
| 7 | Issue #5 — test queueing discussion | Answered and settled in the same thread. | — |
| 8 | PR #4 — a SECOND implementation of logical pause | Closed as superseded, not merged. It was a parallel build of the same feature from another session (`--pair-label` where main uses `-L`), plus a unique LVM-thin design doc now rescued onto main (`3769525`). The branch `claude/opus-vs-fable-5-analysis-fq74yg` still exists if anything else is wanted from it. | owner: confirm the close, or say what else to salvage |
| 9 | Issue #3 — enrolment contract consensus | Open since 2026-08-03, untouched since. | owner+reviewer |
| 10 | Review numbering | **Done**: the closure review is now `REV-20260806-048-ALERT-DELIVERY-CLOSURE.md`; 047 stays with pair-gate logging, which has a response and links. | — |
| 11 | Profiles + live recursion (backlog) | Paused mid-discussion 2026-08-04. Needs a live check of ZFS descendant grants before design. | owner |
| 12 | `test/pause` section G false-fails on real hosts | **Not reproducible — the thread was stale.** Fixed 2026-08-03 in `3776a9a` (the account scan glob became overridable and points at a path that cannot match). Re-verified 2026-08-06 by running the suite on BOTH production hosts: 74/74, G1 passing on each. | — |
| 13 | pve2 has no cron config file | **Not reproducible — the thread was stale.** `/etc/zfs-snapshot-all/jobs.pve2.v4.conf` exists and regenerating from it reproduces the account's managed block byte-for-byte (2026-08-07). The only diff is the `zfs-backup-host` block, which `deploy.sh` owns and gen-cron correctly does not emit. Root's crontab holds three host-level lines and no managed block, so nothing there needs a config either. | — |
| 14 | `docs/OPS_MONITORING.md` untracked | Sitting in the working tree, authorship unclear. | owner: keep or drop |
| 15 | Two sessions built the same feature the same day | PR #4 and main's slices 1-4 were written in parallel, hours apart, without either knowing. Cost: one complete duplicate implementation with its own tests and response file. | owner: decide how parallel sessions announce what they are taking |

| 18 | Reviewer reviewing a stale `main` | **Adopted 2026-08-07:** reviewer re-fetches latest `main` immediately before verdict/write; REV-053 closure was performed only after a fresh commit-list check confirmed `c072e19` as head. | — |
| 19 | REV-051 — dot-segment gate labels | **CLOSED** 2026-08-07 with the negative control accepted. | — |
| 20 | REV-053 — runbook accuracy | **CLOSED** by reviewer 2026-08-07 after direct diff verification of `54c6917`; both F1 and F2 accepted. | — |
| 21b | REV-054 — implemented | All six acceptance conditions met in `ba24c8b` (field + getopts-equivalent reject + cron2conf round-trip) and `121892f` (draft UX + live migration proof). run.sh 58→65, cron2conf 10→11, draftscope 26→28, every other impacted suite re-run green, negative controls against `5241218`/`55acd5c`. Response: `docs/internal/reviews/responses/REV-20260807-054.md`. | reviewer: verdict |
| 21c | Fleet migration to `recursive` | `192.168.11.11` is the one managed config with `-r` in `flags`. One-line edit, proved byte-identical against the **installed** block in both directions (2026-08-07). **v4.27 is already deployed there** via the hourly git pull, so its own config is now non-regenerable — but nothing regenerates crontabs on a schedule (checked on all four hosts), the installed block keeps running, and the refusal only appears on a manual `gen-cron.sh`. Not applied: it is a production file. | owner: apply it, or say when |
| 21e | REV-055 — CHANGES REQUIRED, already fixed | Reviewed `ba24c8b`; both findings were delivered in `121892f`, the next commit. F1 (draft UX) and F2 (pve1 fixture) exist. F2's equivalence WAS weaker than asked — proven transitively, not against the installed block — and that is now completed directly, both directions. Response: `docs/internal/reviews/responses/REV-20260807-055.md`. | reviewer: verdict |
| 21f | Thread #18 recurred | REV-055 was written against a head one commit stale, one day after #18 was marked adopted. Noted, not disputed: the finding was correct about the head it named. | reviewer: whether #18 needs a stronger mechanism than intent |
| 21d | `cron2conf.sh` cannot parse `snapget` lines | Pre-existing, found while building the REV-054 fixture: pull lines are rejected as "unrecognized job line", so the pull half of any round-trip is untestable. Not caused by, and not widened into, REV-054. | reviewer: is this worth a review of its own |
| 21 | Recursion is three decisions, not one | **APPROVED FOR IMPLEMENTATION** by `REV-20260807-054`. Contract: typed `[dataset:] recursive = no|flat|atomic`; legacy `-r`/`-R` in dataset `flags` must hard-fail, including bundled short options; mapping must cover send/pull, prune, monitor, cron2conf round-trip and draft UX. No engine campaign required unless runtime code is touched. | implementer: code to REV-054 acceptance conditions, then respond in `docs/internal/reviews/responses/REV-20260807-054.md` |
| 24 | REV-056 — monitor age rule | **IMPLEMENTED** `1124933`. Age from dataset `creation` through the same ladder, uniformly; provenance in the message; both timestamp reads validated so a failed `zfs get` is UNKNOWN, not an age of the whole Unix epoch. New deterministic `test/monitor` 24/24 (PATH `zfs` stub, no root, no clock races); negative control 14 fail / 10 pass. `check-snap-age.sh` had no suite and a `deps.conf` note claiming it needed none — replaced, because the goldens pin the cron line, not the running script, and both defects lived in that gap. Response: `responses/REV-20260807-056.md`. | reviewer: verdict |
| 25 | REV-057 — migration path | **IMPLEMENTED** `5aafb9f`+`23d616b`+`103cf09`: `gen-cron.sh --migrate-recursion`. Detection reuses the generator's option walker; three-way comparison with the control first; transactional commit; idempotent; crontab never touched. Fleet preflight done — only `192.168.11.11` needs it. `test/migrate` 34/34 local, 36/36 on Linux as the delegated account. Response: `responses/REV-20260807-057.md`. | reviewer: verdict |
| 27 | Migration control skips when run as root | Found by performing 25b. Configs are `root:root 644` in `/etc`, so the delegated account cannot write them and the migration must run as root — but the installed-block control reads the CURRENT user's crontab, and the managed block belongs to the account. The strongest check is therefore unavailable in the only configuration this fleet has. No bad outcome (the before/after render check still ran, equivalence was proven by rehearsal and re-verified by hand), but the guarantee came from the operator, not the tool. Proposed fix in `responses/REV-20260807-057.md`: use the block's own `# Source:` line to find the right crontab, then abort rather than notice if none matches. Not implemented — not redesigning an approved contract unasked. | reviewer: verdict on the fix |
| 25b | The last live step | **DONE** 2026-08-07 14:42. `192.168.11.11` migrated to `recursive = atomic`; crontab md5 unchanged, owner/mode preserved, rollback copy kept, render identical to the installed block, config accepted by the deployed v4.28. **No config in the fleet carries legacy recursion any more.** | — |
| 26 | Delegated-account run found a real defect | `test/migrate` passed 34/34 as root and 24/34 as `zfsbackup`: the installed-block control compared against whatever block the CURRENT USER had, so migrating any second config aborted with an untrue "pre-existing drift". Fixed in `23d616b` via the block's own `# Source:` line. Recorded because this is the third time root-vs-account has hidden a defect. | — |
| 23 | Monitor alerts on a dataset too new to be late | `check-snap-age.sh` calls "has snapshots, none matching" CRITICAL without asking how long the dataset has existed, so a guest created at 15:00 that gets a pvesr/vzdump/manual snapshot before its first backup lands as a false "stale" line in the daily digest. Reachable on 192.168.11.11 today (its children already carry `__replicate_` snapshots); reproduced on scratch. Owner has decided the shape: substitute the dataset's creation time for the missing snapshot's age and reuse the existing severity ladder — **uniformly, no new config field**. Proposal + the four contract details still to settle: `docs/design/monitor-dataset-age.md`. | reviewer: settle wording, UNKNOWN-on-unreadable-creation, and the test home |
| 22 | pve0 has guests with zero backups | Found while surveying for #21. VM 104 `debian` is **running** with 0 `automated_*` snapshots on `hdd/data/vm-104-disk-1`; VMs 103/107 and CT 105 (all stopped) likewise. The config enumerates datasets, reality grew, nothing reconciles them — same shape as the VM 101 gap. Not fixed by #21. | owner: decide whether to just add them, or ask for scope reconciliation as a feature |

## How to use this

- Moving a thread means editing its row in the same commit as the work.
- A new review, discussion or decision gets a row the day it appears.
- "Whose move" is the whole point: a thread with nobody's name on it is
  either finished or forgotten.
